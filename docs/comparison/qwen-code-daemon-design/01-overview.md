# 01 — Overview

> [← 返回 README](./README.md) · [下一篇：Design Decisions →](./02-architectural-decisions.md)

## TL;DR

**qwen serve** 是 Qwen Code 的 HTTP daemon 模式——把 ACP NDJSON 协议通过 HTTP+SSE 暴露成可被任何 client / orchestrator 消费的服务。

**核心架构**：

```
1 daemon process = 1 workspace × N sessions multiplexed
```

与 `qwen --acp` stdio **1:1 心智对齐**——daemon 把 ACP stdio 包装成 HTTP。多 workspace 部署 = 多 daemon process（systemd / docker / k8s 各 1 process 自然管理）。

**关键设计依据**：
- **OS 进程级隔离**：跨 workspace = 跨 daemon process = 跨 OS process（最强）
- **资源 quota 直接对应**：systemd `MemoryMax=` / cgroup / docker `--memory` 直接 = per-workspace quota
- **K8s 云原生天然契合**：1 pod = 1 daemon = 1 workspace
- **Blast radius 最小**：daemon crash 只影响 1 workspace
- **Observability 直接**：`htop` / `ps` 列表 1 OS process = 1 workspace
- **心智简单**：daemon ↔ session 两层（无中间抽象）

**两种部署**：
- **Mode B** `qwen serve` — 当前主线：headless HTTP front，所有 client 通过 HTTP/SSE 接入同一 daemon runtime。
- **Mode A** `qwen --serve` — 2026-05-15 后暂停推进；作为 parking lot 保留，待 Mode B event/control/client contract 稳定后再评估。

**主线 scope**：daemon building block + 协议表面锁定（Stage 2 后）。多 tenant / 跨 daemon process 路由 / SaaS 部署属 **External Reference Architecture**（外部商业平台实施）。

---

## 一、术语表

| 术语 | 定义 | 源码 anchor |
|---|---|---|
| **Daemon process** | `qwen serve` HTTP front 进程；启动时绑定 cwd = 单 workspace；持 Express 5 server + EventBus + 1 个内嵌 `qwen --acp` child | `packages/cli/src/serve/server.ts` |
| **Session** | ACP `Session` 实例（`QwenAgent.sessions: Map<sessionId, Session>` 内一条）；持 transcript / FileReadCache / PermissionManager | `packages/cli/src/acp-integration/session/Session.ts` |
| **Daemon client / adapter** | TUI / channel / web / IDE / JSONL 等 client 通过 `DaemonClient` / `DaemonSessionClient` + typed event reducer 接入，不直接订阅内存 EventBus | 详 [§02 §8](./02-architectural-decisions.md#8-server--client--runtime-boundary2026-05-18) |
| **Runtime worker** | 真正执行 tool / shell / MCP / skills / LSP / file operations 的 runtime；当前是 `qwen --acp` child，未来可替换 sandbox runner | 详 [§02 §8](./02-architectural-decisions.md#runtime-worker--sandbox-runner-boundary) |

**核心约束**：
- 1 daemon process **严格 1 workspace**（启动 cwd 绑定）
- 1 daemon process **可持 N sessions**（`QwenAgent.sessions: Map` 多路复用）
- 同 workspace N session 共享同一个 `qwen --acp` child process；当前 `Config` / `FileReadCache` / `ToolRegistry` / `McpClientManager` 仍随 ACP session 创建，跨 session MCP 共享需要未来 pool/proxy。

> 代码中可能看到的废弃符号：`byWorkspaceChannel` / `ChannelInfo` / `Workspace Bridge`（PR#3889 multi-workspace 路由层，已由 [PR#4113](https://github.com/QwenLM/qwen-code/pull/4113) 移除）。

---

## 二、架构图

```
qwen serve (1 Daemon process, 绑定 cwd = /work/repo-a)
├─ Express 5 HTTP server + bearer auth + Host allowlist
├─ EventBus（per-session fan-out + ring replay + Last-Event-ID 重连）
└─ qwen --acp child (workspace = /work/repo-a)
   ├─ QwenAgent.sessions: Map → {sess-1, sess-2, sess-3}
   └─ per-session Config / MCP manager / FileReadCache
```

**多 workspace 部署**（开发者本地多项目 / 多 user / 多 tenant）：

```
┌────────────────────────────────────────┐
│ 同机多 daemon 部署                       │
├────────────────────────────────────────┤
│ qwen serve (cwd=/work/repo-a) :8001    │  ← daemon-1
│ qwen serve (cwd=/work/repo-b) :8002    │  ← daemon-2
│ qwen serve (cwd=/work/repo-c) :8003    │  ← daemon-3
└────────────────────────────────────────┘
                ↓ systemd / docker-compose / k8s
        OS-level 进程隔离 + per-daemon cgroup quota
```

跨 workspace 协调（IM bot 多项目 / WebUI 多 workspace 概览 / IDE multi-root）由 **client 侧 / orchestrator 侧**做（多 daemon endpoint 发现 + 路由）——daemon 自身只管"当前 workspace"。

**2026-05-18 架构边界补充**：

```text
client adapters / output sinks
  → daemon client/protocol layer
  → qwen serve HTTP/SSE control plane
  → runtime worker / sandbox runner
```

TUI / web terminal / channel / IDE / JSONL / stream-json 都应成为 daemon-native consumers：消费 typed events + shared reducer，再投影到各自 UI 或输出格式。PTY proxy 只保留为兼容 / demo / debug fallback。remote-control 是 control overlay，不再拥有独立 runtime / event protocol。详 [§02 §8](./02-architectural-decisions.md#8-server--client--runtime-boundary2026-05-18)。

External Reference Architecture 提供 orchestrator 层（详 [§06 §五 External Reference Architecture](./06-roadmap.md)）。

---

## 三、双部署模式

| 模式 | 启动命令 | TUI | 适用场景 |
|---|---|---|---|
| **Mode B: Headless + HttpServer** | `qwen serve [--port N]` | ❌ | 当前主线：服务器 / 容器 / 远端机器 / K8s pod / 所有 client 的统一 runtime |
| **Mode A: CLI + HttpServer** | `qwen --serve [--port N]` | ✅ 本地 | 暂停推进；作为 parking lot 保留 |

**当前 client 边界**：TUI / channels / web / IDE 直接对接 `qwen serve` HTTP server；`GET /session/:id/events` 是 daemon 内部 EventBus 的 SSE projection。外部 client 不直接 import / subscribe 内存 EventBus。

**Mode B 远端 client 是 "thin shell"**（Stage 1）——只能渲染 wire 流，daemon-side state dialogs（`/memory` / `/mcp` / `/agents` 等）不可用。Stage 1.5c daemon-side state CRUD 补齐后，TUI / channels / web / IDE 才能成为完整 client。详 [§04](./04-deployment-and-client.md)。

### 三·一 Deployment forms（来自 chiga0 [#3803 comment 4476174099](https://github.com/QwenLM/qwen-code/issues/3803#issuecomment-4476174099)，2026-05-18）

Mode B 之下进一步区分**3 种 deployment form**，钉死 daemon host 与 workspace host 的同址要求：

| Form | Daemon host | Workspace host | Client host | 推荐度 |
|---|---|---|---|---|
| **Local single-machine** | local 机器 | local 机器 | local TUI/IDE/channel | ✅ 主装机体验；daemon 可在 loopback 自动起 |
| **Cloud/devbox remote-runtime** | remote host/pod | **同** remote host/volume | local TUI/IDE/web | ✅ 推荐 cloud+client 拆分；Codespaces/Coder 风格——workspace 与 runtime **必须 colocate** |
| **Local workspace + remote daemon** | remote host/pod | local 机器 | local client | ❌ **不推荐** —— daemon 看不到 local files / tools / MCP / skills，除非有显式 sync / mount / orchestrator |

**核心不变式 — daemon host = runtime host**：

> File access / shell tools / LSP / provider auth / MCP servers / skill discovery+resources+scripts / process execution **全部从 daemon environment 求值**，除非未来有显式的 [client-capability reverse RPC](./04-deployment-and-client.md#六client-capability-反向-rpc) 例外声明。

详 [§04 §四 Deployment shape matrix](./04-deployment-and-client.md#deployment-shape-matrixclientruntime-lens2026-05-18) + [§06 §三·二 Package boundary contract + auto-daemon UX](./06-roadmap.md#三二-deployment--package-contract-chiga0-3803-comment-2026-05-18)。

---

## 四、资源经济性

**同 workspace N session 经济性**（继承 ACP `QwenAgent.sessions: Map` 原生 multi-session）：

| N 同 workspace session | 内存 RSS | 说明 |
|---|---|---|
| 1 | ~60-100 MB | baseline |
| 5 | **~60-100 MB** | N session 共 1 `qwen --acp` child |
| 10 | **~80-150 MB** | 同上 |
| OAuth refresh | 1× per daemon | 共享 |
| FileReadCache | per session | 当前随 `Config` 创建；跨 session 共享未做 |
| CLAUDE.md parse | per session | 当前随 session config 初始化 |
| Cold start（同 daemon 第 N session）| **<200ms** | attach existing child |

**多 workspace 部署成本**：M workspace = M daemon process。每 daemon baseline ~30-50 MB Express server + bearer auth + EventBus + Host allowlist。例 5 workspace × 5 session 同机 ≈ ~450-750 MB（5 × baseline + 5 × child）。换得 OS 进程级隔离 + cgroup quota + blast radius 最小 + 心智简单。

---

## 五、Stage 演进

| Stage | 范围 | 状态 |
|---|---|---|
| **Stage 1** | Mode B headless `qwen serve` + N session multiplexed + EventBus + first-responder permission + 9 STAGE1_FEATURES | ✅ **MERGED 2026-05-13** ([PR#3889](https://github.com/QwenLM/qwen-code/pull/3889)) |
| **Stage 1.5a §02** | [PR#4113](https://github.com/QwenLM/qwen-code/pull/4113) 1 daemon = 1 workspace 收紧 | ✅ **MERGED 2026-05-15** |
| **Stage 1.5a must-haves** **(P0)** | chiga0 10 must-haves 剩 9 项 — Mode B 生产 blocker（loadSession HTTP / pair tokens / sessionScope override）| ~2 周（9 PRs 可并行）|
| **Stage 1.5c** **(P0)** | daemon-side state CRUD 8 routes — Mode B 远端 client 摆脱 thin shell | ~3-5d |
| **Stage 1.5-prereq** **(P1)** | chiga0 6 architecture findings — lift `AcpChannel` / `EventBus` / `PermissionMediator` 到 `@qwen-code/acp-bridge` | ~1-2 周 |
| **Stage 1.5-client adapters** **(P1 behind flag)** | TUI / channels / web/debug / IDE 接入 Mode B daemon；默认切换必须等 P0/P1 | ~2-3 周 |
| **Stage 1.5b** Mode A **(P2 推迟)** | Mode A `qwen --serve` flag — [Issue #4156](https://github.com/QwenLM/qwen-code/issues/4156)；A1 [PR#4160](https://github.com/QwenLM/qwen-code/pull/4160) ✅ MERGED；**推迟到 1.5c 后**（Mode A 价值依赖 1.5c）| ~5-6d |
| **Stage 1.5-remote-control** **(P2 后置)** | [PR#3929](https://github.com/QwenLM/qwen-code/pull/3929) / [#3930](https://github.com/QwenLM/qwen-code/pull/3930) / [#3931](https://github.com/QwenLM/qwen-code/pull/3931) 后续作为 daemon facade | 待 primary clients 收敛 |
| **Stage 2** | 协议补齐（WebSocket / mDNS / OpenAPI / Prometheus / `/ext` + Reverse RPC 5 类 Client Capability）| ~3-4 周（拆 2a-2d）|
| **Stage 2e** | 可选 native in-process（去 `qwen --acp` child）| ~1-2 周 |

> **优先级决策（2026-05-15）**：Mode B 优先 — must-haves + 1.5c 摆脱 thin shell → 1.5-prereq 架构清洁 → Mode A（1.5b）推迟。详 [§06 §三 推进顺序](./06-roadmap.md)。
>
> 💡 **Implementation tracker**：[Issue #4175](https://github.com/QwenLM/qwen-code/issues/4175) doudouOUC 的 Mode B v0.16 production-ready 31-PR rollout plan（7 Wave，含 Wave 2.5 reliability）—— 上表的 Stage 1.5a/c/-prereq 映射到 Wave 1-5；Wave 6 是 release hardening + v0.16。详 [§06 §三·一 Wave breakdown](./06-roadmap.md#三一-issue-4175--31-pr-wave-breakdown-production-ready-tracker)。
>
> 🎉 **Wave 1+2+2.5+3+4 + W5 PR 22a+22b/1+22b/2 + 多 follow-up 同夜冲合**（2026-05-16~19 共 **38 MERGED + 7 OPEN/draft + 3 CLOSED**）；
>
> 📦 **产品 v0.16.0 已发布（2026-05-21，[PR#4404](https://github.com/QwenLM/qwen-code/pull/4404)）**：semver 0.15.11 → 0.16.0 全 workspace 包 bump（含新 `acp-bridge` 包）。v0.16.0 **在 main 上**含 daemon **Wave 1-4 全部 + Wave 5 PR 22-series**；Wave 5 **F-series（F1/F2/F3 + F4 prereq）在 `daemon_mode_b_main` 分支上尚未周期 merge → 不在 v0.16.0**。⚠️ 产品 semver v0.16.0 ≠ daemon Wave plan 的 "PR 31 v0.16 production-ready" 里程碑（后者是 daemon Mode B 功能完备宣告，详 [§06 Wave 6](./06-roadmap.md)）。
>
> 🆕 **maintainer 分支策略重组（2026-05-19）**：剩余 Mode B 工作（PR 22b/3 + 22b' + PR 23/24/25/26）不再走 main，而是**拆为 F1-F5 feature PRs** target 长期 integration 分支 `daemon_mode_b_main`；最终通过 `daemon_mode_b_main → main` 周期 merge PR 触发 full CI matrix。**F-series + chiga0 SDK 侧并行 track + ytahdn web-shell 当前状态（截至 2026-05-27）**：

**doudouOUC daemon 侧（F-series + 后续）**：F1 [#4319](https://github.com/QwenLM/qwen-code/pull/4319) ✅ + F1 follow-up [#4334](https://github.com/QwenLM/qwen-code/pull/4334) ✅ + F1 test split [#4445](https://github.com/QwenLM/qwen-code/pull/4445) ✅ + F2 [#4336](https://github.com/QwenLM/qwen-code/pull/4336) ✅ + **F2 cleanup PR A [#4411](https://github.com/QwenLM/qwen-code/pull/4411) ✅ MERGED 05-23**（pure-refactor）+ **F2 cleanup PR B [#4460](https://github.com/QwenLM/qwen-code/pull/4460) ✅ MERGED 05-23 15:23**（W133-a + W134 self-heal observability）+ F3 [#4335](https://github.com/QwenLM/qwen-code/pull/4335) ✅ + F4 prereq [#4360](https://github.com/QwenLM/qwen-code/pull/4360) ✅ MERGED 05-21（daemon protocol completion）+ **#4514 backlog 关 2 个**：T2.4 CORS [#4527](https://github.com/QwenLM/qwen-code/pull/4527) ✅ MERGED 05-26 12:16 + T2.9 prompt deadline+idle [#4530](https://github.com/QwenLM/qwen-code/pull/4530) ✅ MERGED 05-26 17:09；**#4175 inventory 关 #1**：/recap [#4504](https://github.com/QwenLM/qwen-code/pull/4504) ✅ MERGED 05-26 07:09；**daemon file logger [#4559](https://github.com/QwenLM/qwen-code/pull/4559)** ✅ MERGED 05-27 06:05 关 #4548；**F5 release chain（v0.16-alpha cut）**：PR 27 docs [#4473](https://github.com/QwenLM/qwen-code/pull/4473) ✅ + PR 30a local launch templates [#4483](https://github.com/QwenLM/qwen-code/pull/4483) ✅ + PR 28 npm publish + PR 31 cut 待开；**周期 sync**：[#4469](https://github.com/QwenLM/qwen-code/pull/4469) (05-24, 45 commits) + [#4500](https://github.com/QwenLM/qwen-code/pull/4500) (05-25, 5 commits) 拉 main 入 integration 防 F5 链每个背 delta；**#4514 T1.3+T1.4 [#4516](https://github.com/QwenLM/qwen-code/pull/4516) ❌ CLOSED self-close 05-26 09:25** —— doudouOUC 重 triage 后撤回：compress route 已可经 `/prompt` slash passthrough，`_meta` route 当前非 must-have，需 client demand 驱动 reopen；**T1.3+T1.4 取消是个架构 lesson**：backlog "missing capability" 标记需 client demand 验证不是 wire surface gap 就 implement。

**chiga0 client-facing surface 双轨 + side-channel coordination (#4511) track**：① **SDK daemon UI 层**：[PR#4328](https://github.com/QwenLM/qwen-code/pull/4328) ✅ MERGED 05-22 + [PR#4353](https://github.com/QwenLM/qwen-code/pull/4353) ✅ MERGED 05-24（#4328 55% → 95%，~4d 17h 24 commits / 61 reviews，R1-R7 收尾 security hardening）+ **第 4 PR cross-client real-time sync 5 fixes [#4484](https://github.com/QwenLM/qwen-code/pull/4484) ✅ MERGED 05-25 09:28**；② **ACP 生态接入**：[PR#4472](https://github.com/QwenLM/qwen-code/pull/4472) 🔧 OPEN REVIEW_REQUIRED 持续 expand 到 +6098/-316 20 files；③ **side-channel coordination [#4511](https://github.com/QwenLM/qwen-code/issues/4511) design 实现 track**：A1 in-session model switch [#4546](https://github.com/QwenLM/qwen-code/pull/4546) ✅ MERGED 05-26 16:06 + A4 voterClientId [#4539](https://github.com/QwenLM/qwen-code/pull/4539) ✅ MERGED 05-26 09:54 —— chiga0 PR 数已达 6 个。

**ytahdn web-shell consumer track**：daemon 团队第 3 作者（继 doudouOUC + chiga0）—— ① [PR#4380](https://github.com/QwenLM/qwen-code/pull/4380) `Feat/daemon react cli` **✅ MERGED 2026-05-27 01:42**（创建 05-21 06:07 历时 ~6 天，**+17121/-66**，新 `packages/web-shell` daemon-backed React web-shell，**第一个真实 web client 消费 chiga0 SDK daemon UI 层 #4328/#4353 验证整 stack**）；② **第 2 PR 同日 9.5h 后开** [PR#4573](https://github.com/QwenLM/qwen-code/pull/4573) `feat(web-shell,webui,sdk): context-usage API + daemon-react-sdk refactor + dialog UX` 🔧 OPEN CHANGES_REQUESTED 2026-05-27 11:13（+9820/-3713 119 files, target `daemon_mode_b_main`）—— 新 `GET /session/:id/context-usage` 路由全链路 + 新 `session_context_usage` capability tag + webui daemon provider 模块化拆 `session/`+`workspace/` 子模块 + 新 `@qwen-code/webui/daemon-react-sdk` subpath export + 11 dialog UX (ESC 按钮 + keyboard scope) + security fix (Mermaid strict / Shift+Tab no silent yolo / per-tab clientId via sessionStorage)。**Wave plan 进度名义 22.75/31 ≈ 73%，但剩余工作已重组**：
> - ✅ Wave 1 **PR 2** [PR#4191](https://github.com/QwenLM/qwen-code/pull/4191) capability registry + protocol versions — **MERGED 2026-05-16 10:07** (doudouOUC)
> - ✅ Wave 2 **PR 5** [PR#4209](https://github.com/QwenLM/qwen-code/pull/4209) per-request `sessionScope` override — **MERGED 2026-05-16 15:54** (doudouOUC, 4h26m open→merge)
> - ✅ Wave 1 **PR 1** [PR#4205](https://github.com/QwenLM/qwen-code/pull/4205) baseline harness — **MERGED 2026-05-16 16:41** (doudouOUC, 4 Critical 修后；首份 `baseline-stage-1.json` 存档 macOS arm64 / RSS 223.5 MB / attach 1-3 ms / MCP 4 children constant under single-scope)
> - ✅ Wave 1 **PR 3** [PR#4201](https://github.com/QwenLM/qwen-code/pull/4201) DaemonSessionClient skeleton — **MERGED 2026-05-16 17:01** (chiga0；前身 PR#4195 CLOSED；v2 补 AbortSignal/event-without-id/error-path 测试)
> - ✅ Wave 2 follow-up [PR#4214](https://github.com/QwenLM/qwen-code/pull/4214) — **MERGED 2026-05-16 17:51** (doudouOUC, 1h23m) — 校准 integration-test 9→10 + 建立 capability registry **三套来源 lockstep** 模式
> - ✅ Wave 1 **PR 4** [PR#4217](https://github.com/QwenLM/qwen-code/pull/4217) typed event schema — **MERGED 2026-05-17 04:31** (chiga0, squashed `ab88f6f1`，5 轮 review / 2 轮自我修复：① 4 Critical 修 ② 5 项 reducer hardening invariants：`lastEventId` 单调推进抗回退 / `pendingPermissions` cap 64/session + dropped 诊断计数 / `permission_resolved` 未匹配 requestId 诊断 + cancelled outcome / `toolCall` 校验收紧 + finite number / reducer 存储时复制抗引用共享)
> - ✅ Wave 2 **PR 6** [PR#4222](https://github.com/QwenLM/qwen-code/pull/4222) HTTP load/resume session — **MERGED 2026-05-17 04:58** (doudouOUC `[codex]`, 3h17m open→merge, 5 轮 review；体量 review 期间几乎翻倍 +1091/-35 → **+2078/-51 16 文件**；wenshao 03:33 第一次 APPROVE 后 11 分钟二次 /review 发现 **asymmetric coalesce guard** 等 2 Critical 翻回 CHANGES_REQUESTED——原只 guard load-on-resume，因 `resume()` seeds `lastEventId: 0` 与 load 语义不同必须双向；commit `db55b1c8b` 修后 04:55 最终 APPROVE)
> - ✅ Wave 2 **PR 7** [PR#4231](https://github.com/QwenLM/qwen-code/pull/4231) daemon-stamped client identity — **MERGED 2026-05-17 08:19** (chiga0, 1h23m open→merge, 最终 +910/-79；wenshao **9 parallel review agents** 覆盖 correctness/security/code quality/performance/test coverage + 3 audit personas；commit `52295980f4` 修 1 Critical（`InvalidClientIdError` 走 `sendBridgeError` 翻 400 而非 500）+ 6/7 其他 concern；`randomUUID()` + `client_` 前缀 122 bits entropy)
> - ✅ Wave 2 **PR 8** [PR#4232](https://github.com/QwenLM/qwen-code/pull/4232) session-scoped permission route — **MERGED 2026-05-17 09:48** (chiga0, 2h44m open→merge, 最终 +801/-30；rebase 到 main 后 single-commit `4eae97a33b`；review 抓 3 项：barrel re-export 新 type / **抽 `parsePermissionOutcome()` 共享 helper** 去掉 scoped+legacy 两 route 重复 / input validation 测试补全；`permission_already_resolved` event + bounded record 防 memory leak（同 PR#4217 cap pattern）+ 新 capability tag `session_permission_vote`)
> - ✅ Wave 2.5 **PR 9** [PR#4235](https://github.com/QwenLM/qwen-code/pull/4235) client heartbeat — **MERGED 2026-05-17 10:57** (doudouOUC, 2h00m open→merge，**首轮一把过 0 CHANGES_REQUESTED**；+581/-2 14 文件, 测试 / 生产 ≈ 1.7x, 14 test cases；wenshao quote："**all 3471 serve/SDK tests pass** ... security model is sound: clientId validation runs before any timestamp update"；**4 安全不变式** + lockstep 维持 + HTTP state route 推迟到 PR 12 + `client_heartbeat` capability tag —— 把 chiga0 5 轮 review 学到的全 internalize 进首版)
> - ✅ Wave 2.5 **PR 10** [PR#4237](https://github.com/QwenLM/qwen-code/pull/4237) SSE replay sizing + `slow_client_warning` backpressure — **MERGED 2026-05-17 11:30** (doudouOUC, 2h19m open→merge, 3 轮 review (0 Critical, 3+5 Suggestion)；最终 +893/-81；wenshao 高赞 **`BoundedAsyncQueue.liveCount` refactor** 是 thoughtful pre-emptive fix——原 `forcedInBuf` counter 在 `slow_client_warning` mid-stream force-push 下会挂，doudouOUC 自己实现时意识到 invariant 破裂提前重构；`DEFAULT_RING_SIZE` 4000→8000 + 5 处 docs 文案修 + `eventRingSize` forwarding + hysteresis 75%/37.5% 防 flap)
> - ✅ Wave 2.5 **PR 11** [PR#4240](https://github.com/QwenLM/qwen-code/pull/4240) session metadata + close/delete lifecycle — **MERGED 2026-05-17 12:42** (doudouOUC, 2h16m, 4 轮 review, 最终 +1175/-35；headRef `worktree-delightful-cooking-goose` Claude Code worktree fingerprint；1 Critical typecheck 修（`BridgeSessionSummary` 新字段 + mock 同步）+ ordering 修（`events.close()` 必须 BEFORE `connection.cancel()` in `closeSession`，否则 subscriber 收 `session_closed` 前流就断）；wenshao 高赞 "**terminal-event upgrade + pending-permission cancellation 的 test coverage exactly the contract I'd worry about most**")
> - ✅ Wave 3 **PR 12** [PR#4241](https://github.com/QwenLM/qwen-code/pull/4241) read-only status routes — **MERGED 2026-05-17 13:37** (doudouOUC, 3h02m，最终 +2363/-64 review +251 LOC；wenshao **review pipeline 又升级**：gpt-5.5 /review + **mimo-v2-5-pro 9 parallel agents**（新加 **attacker simulation + 3AM oncall** personas）+ 增量审 + force-push hardening commit `6244c894` 修；cli vitest 368 → **396 tests passing（+28）**；关键修复：read-only session routes 也 forward `clientId` for **audit symmetry**——把 PR#4231 invariant 扩到 read 路径；workspace routes 不 spawn ACP 即使 polled；5 routes + 5 capability tags + SDK 5 typed payload types)
> - ✅ Follow-up [PR#4245](https://github.com/QwenLM/qwen-code/pull/4245) integration test mirror align — **MERGED 2026-05-17 14:53** (doudouOUC, **40m open→merge 一把过**——daemon 项目最短；release CI run 25992130532 跑挂触发；features list 18→24 + SSE backpressure 3→4 frames 校准；wenshao 9-dimension review 全通过 + post-merge gpt-5.5 二次验证；**第三次 drift 警告**——doudouOUC 建议把 integration test 进 PR-CI lane 或 restructure assertion 去掉 inline expected list)
> - ✅ Wave 3 **PR 13** [PR#4251](https://github.com/QwenLM/qwen-code/pull/4251) preflight + env diagnostics routes — **MERGED 2026-05-17 23:29** (doudouOUC, 7h40m, 7 轮 review；最终 +2577/-127 (review +684/-98)；**第六次 drift 被 wenshao [Critical] 在 PR review 中抓**——doudouOUC "故意 batch fix" 策略破产，必须 in-PR 修；4 轮 fix: ① **execSync → execFile migration** 安全防 shell injection（也修 Windows CI 挂）② `safeCheck` errorKind ③ proxy redaction `safeProxyValue` 3-stage parse (URL → `http://`-prepend → regex scrub → `<unparseable>` final fallback + 4 unit tests) ④ `validateAuthMethod` mutation P1 ⑤ integration test mirror 加 `workspace_env` + `workspace_preflight` ⑥ live ACP + ACP failure fallback tests（之前只测 idle path）⑦ 3 structural fix from low-confidence items；2 route + 第一个 land **closed `errorKind` 7-value taxonomy**（PR 14 复用）+ 5 strict invariants（含 build-time grep）+ 6 commit 拆分；639/639 tests passed)
> - ✅ Wave 3 **PR 14** [PR#4247](https://github.com/QwenLM/qwen-code/pull/4247) MCP client guardrails — **MERGED 2026-05-18 04:07** (doudouOUC, **13h09m daemon 项目史上最艰难一合**, 9+ 轮 review **11 [Critical]**)；最终 +2867/-31（review 翻 2.4x +1679 LOC）；**4 个不同 LLM model 跑 review**: mimo-v2.5-pro / DeepSeek-v4-pro / glm-5.1 / 9 parallel agents + 3 reverse-audit rounds；R2 短暂 APPROVED 但 R3 翻回 CHANGES_REQUESTED；wenshao 中文最终 APPROVE quote: "前次 16:18 APPROVED 落后 5 个 commit；我陆续追了 11 条 [Critical]（R3-R8 各轮）。HEAD 全部对账通过——3 个 spawn path（bulk/per-server/lazy `readResource`）现在 **`weReserved`-driven 清理完全对称**"；核心修复 `if (weReservedSlot) { try { await client.disconnect() } catch {} reservedSlots.delete() }` 模式贯穿所有 catch 块；3 spawn path budget gating 全补 (C1) + zombie slot leak 全修 (C2/C3) + transport leak 修 (R7-R8) + stale global discoveryState 改 instance-bound + 197/197 tests passing；体量与 review 轮次创 daemon 项目 4 项最高纪录)
> - ✅ Wave 4 **PR 16** [PR#4249](https://github.com/QwenLM/qwen-code/pull/4249) workspace memory + agents CRUD — **MERGED 2026-05-18 06:27** (doudouOUC, **15h20m 创新 daemon 项目最长 lifetime** 破 PR#4250 13h55m；**11 rounds review + 6 LLM model**：Copilot / gpt-5.5 / DeepSeek-v4-pro / glm-5.1 / qwen-latest / github-advanced-security + wenshao R1-R7；最终 +5318/-8 23 文件（review 翻 +2364）；wenshao R11 quote: "**fold-in cascade 2a → 2j has addressed every critical and high-impact finding**"——10 sub-fix iteration；7 routes + 2 cap tag + 2 typed event workspace-level fan-out + 3 scope decision (auto-memory deferred PR 16.5 / agents create+update split / persistent audit log deferred PR 24)；工程亮点 CRUD-scoped `Config` Proxy stub fail-loud + whitespace-append suppression + bridge.publishWorkspaceEvent fan-out)
> - ✅ Wave 4 **PR 19** [PR#4269](https://github.com/QwenLM/qwen-code/pull/4269) safe workspace file read routes — **MERGED 2026-05-18 08:17** (doudouOUC, **1h54m 极短 lifetime**——PR 18 chokepoint 红利显化；最终 +1454/-8 10 文件；wenshao 中文 APPROVED quote: "本地实测通过(525/525 tests + **安全边界全部正确拒绝** + **审计串联确认**)"；4 routes `GET /file|/list|/glob|/stat` 通过 PR 18 `WorkspaceFileSystem` boundary；单 cap tag `workspace_file_read`；wire `runQwenServe` 构 + inject `fsFactory`——**PR 18 `trusted: false` default invariant 第一个真 consumer 验证**；关闭 PR 18 follow-up #2 + #4)
> - ✅ Follow-up [PR#4279](https://github.com/QwenLM/qwen-code/pull/4279) Windows 路径 hotfix — **MERGED 2026-05-18 09:53 (26m 极短，daemon 项目最快 hotfix 破 PR#4245 40m)** (doudouOUC, +7/-1 1 文件；`workspaceRelative` 漏 `path.sep`→`/` normalize，Windows CI 唯一 red 1h36m on main；4 routes `/file`/`/stat`/`/list`/`/glob` 全受影响；wenshao gpt-5.5 /review 一把过)
> - ✅ Wave 4 **PR 20** [PR#4280](https://github.com/QwenLM/qwen-code/pull/4280) file write/edit + bounded raw byte read — **MERGED 2026-05-18 14:37** (doudouOUC `[codex]`, **4h47m + 仅 13 reviews**——PR#4255 巅峰后回归常态；最终 +2557/-266 25 文件；4 块新功能：bounded raw byte reads (补 PR#4269 binary case) / hash-bearing text reads / strict-auth write+edit 经 PR 15 gate / content-hash concurrency check；**4 关键工程亮点**：① serve-local **atomic temp+rename helper** (temp write → fsync → hash precondition → rename → fsync dir) ② **expected-hash precondition** 防 stale write 丢失更新 ③ **edit single-match policy** `oldText` 必须 exactly 1 处 match (零/多 match 都报错，让 caller disambiguate) ④ `X-Qwen-Client-Id` 强制 stamp audit on write/edit；12:58 wenshao CHANGES_REQUESTED → 13:33 修 → 14:17 gpt-5.5 /review APPROVED → 14:37 MERGED)
> - ✅ Wave 4 **PR 17** [PR#4282](https://github.com/QwenLM/qwen-code/pull/4282) approval/tools/init/MCP-restart — **MERGED 2026-05-18 16:27** (doudouOUC, **6h21m + 34 reviews**——Wave 4 收尾常态；最终 +3685/-13 28 文件 (review +939)；**4 strict-gated mutation routes**：`POST /session/:id/approval-mode` (`plan`/`default`/`auto-edit`/`yolo` + optional `persist: true`) + `POST /workspace/tools/:name/enable` (`tools.disabled` skip-register **distinct from** `permissions.deny`) + `POST /workspace/init` **纯机械** scaffold `QWEN.md` 故意不调 model（保 mutation 同步语义 vs prompt 异步语义清晰；AI-fill 需 client 显式 follow-up `POST /session/:id/prompt`）+ `POST /workspace/mcp/:server/restart` 带 PR 14 v1 budget pre-check；全 strict-gated + `X-Qwen-Client-Id` audit + `originatorClientId`-stamped SSE event；**核心 hardening**：`TrustGateError` typed class in core → bridge map untrusted-folder rejection to `errorKind: 'auth_env_error'` (PR 13 taxonomy) **不再 regex-match messages**——跨 PR typed-error 模式复用；wenshao 14:59 CHANGES_REQUESTED → 15:51 doudouOUC inline 修 → 16:27 APPROVED + MERGED) — **Wave 4 完整 7/7 收尾 🎉**
> - ✅ Wave 5 **PR 22a** [PR#4295](https://github.com/QwenLM/qwen-code/pull/4295) acp-bridge skeleton + zero-coupling primitives lift — **MERGED 2026-05-18 17:23** (doudouOUC, **56 分钟极速合**——pure refactor 红利；最终 +1106/-688 17 文件，review 期间仅 -20/+44 极小调整；8 reviews；wenshao APPROVED quote: "**pure refactor, mechanically verified**: file moves preserve content via `git mv`, wrappers are two lines (docstring + `export *`) so **no opportunity for behavioral drift**, all **13 existing import sites resolve through the wrappers unchanged**, the new package builds and passes 28/28 of its own tests, and the daemon's SSE / ring replay / Last-Event-ID..."；3 zero-coupling primitive 移到新 `@qwen-code/acp-bridge` package；额外种 `PermissionMediator` interface contract 给 PR 24 4 strategies；chiga0 #3803 "Stage 1.5-prereq AcpChannel lift" 真正落地；**解锁 PR 22b / PR 23 / PR 24**；follow-up suggestion: `SubscriberLimitExceededError` (public DoS defense) 测试覆盖待补)
> - ✅ Follow-up [PR#4291](https://github.com/QwenLM/qwen-code/pull/4291) PR#4255 OAuth fold-in — **MERGED 2026-05-18 23:01** (doudouOUC, **7h35m**, 最终 +1406/-31 (初版 +418/-20，**review 期间 +988 ×3.4 涨**)；同时打 post-merge deepseek-v4-pro 5 项 (含 2 Critical: `poll()` raw err.message stderr / `runPollTick` 30s timeout) + PR#4255 PR body 列的 fold-in 1 deferred 5 项 (DeviceFlowEntry discriminated union / single-source status union / `awaitCompletion` memoize / ALL session bus fail stderr elevate / 404 `not_found_or_evicted` errorKind)；**OAuth 一条线总投入 ~+7578 LOC** (PR#4255 +6172 + PR#4291 +1406)，daemon 项目史上最贵功能)
> - ❌ Follow-up [PR#4293](https://github.com/QwenLM/qwen-code/pull/4293) E2E baseline lockstep — **CLOSED 2026-05-18 16:45 (Superseded by PR#4282)** (doudouOUC, 24 分钟内 close；**8th mirror drift** PR 20/21 merged 后 main E2E run 26040351119 跑挂 Linux sandbox:none/docker + macOS 全挂；doudouOUC 16:21 开 +3 一行 fix → 16:27 **PR#4282 W4 PR 17 MERGED 期间 review-time bundle 了 3 cap baseline 一并 sync**——wenshao 主动让 PR 17 顺手做 8th drift 善后 → 16:45 doudouOUC close PR#4293 提"main's E2E baseline is already in sync"；揭示 doudouOUC 真实 lockstep 模式是 **review-time batch consolidation** 而非 per-PR sync——main 短暂破损换每 feature PR 不加 lockstep 负担)
> - 🔧 Follow-up [PR#4297](https://github.com/QwenLM/qwen-code/pull/4297) PR#4282 post-merge P2 fixes — **OPEN + CONFLICTING + CHANGES_REQUESTED**（doudouOUC, 2026-05-18 16:39, **review 期间 +846 ×3.4 涨**到 +1191/-54 (初版 +345/-11)；**PR#4282 W4 PR 17 squash-merge 后 Codex /review 抓 4 P2 issue** 均现有测试套不可重现：① `POST /workspace/init` 用错文件名——daemon parent 不 `loadCliConfig`，修 `runQwenServe` snapshot value 转发 `BridgeOptions.contextFilename` ② `disabledTools` snapshot stale —— restart MCP 用 fresh snapshot ③④...；与 PR#4291 同款 ×3.4 体量膨胀 pattern；daemon 项目工作流升级：**"review at merge" → "review continuously"** 标准动作 post-merge AI /review)
> - ✅ Wave 5 **PR 22b/1** [PR#4298](https://github.com/QwenLM/qwen-code/pull/4298) acp-bridge: lift status / paths / errors / bridge types — **MERGED 2026-05-18 23:00** (doudouOUC, 4h55m, +1431/-1450 12 文件；**PR 22 拆 3 阶段 split**：PR 22a (zero-coupling primitives, MERGED 17:23) + **PR 22b/1 (pure-type + pure-utility surface lift, MERGED 23:00)** + ⏳ PR 22b/2 (implementation lift + `DaemonStatusProvider` injection seam)；mechanical + low-risk lift continuation；3-stage split 让每阶段保持机械低风险，复杂度集中在最后一阶段)
> - ✅ Follow-up [PR#4300](https://github.com/QwenLM/qwen-code/pull/4300) typed errors for channel-closed + missing-cli-entry — **MERGED 2026-05-18 23:14** (doudouOUC, closes #4299 tech-debt；+118/-29 3 files；`BridgeChannelClosedError` + `MissingCliEntryError` 替代 `mapDomainErrorToErrorKind` 的 regex on `.message`——**typed-error 模式跨 PR 第 N 次复用**（PR 13 closed `errorKind` taxonomy / PR 17 `TrustGateError` / PR 22-area BridgeTimeoutError / 现在两个），daemon 项目 typed-error 设计哲学固化)
> - ✅ Follow-up [PR#4302](https://github.com/QwenLM/qwen-code/pull/4302) telemetry Phase 1.5 polish — **MERGED 2026-05-19 01:21** (doudouOUC, 2h14m open→merge, 最终 +454/-65 7 files **review 期间 +199 LOC 涨** 255→454; merge commit `8d9cc6156b`；wenshao 1 轮 CHANGES_REQUESTED → DISMISSED → gpt-5.5 /review LGTM → qwen-latest /review APPROVED；4 fix items：① `session-tracing.ts` 新 `resolveParentContext()` mirrors `tracer.ts:getParentContext()`, ALS parent 缺时 re-parent 到 active OTel span 再 fallback 到 synthetic session root (3 call site 统一: `startLLMRequestSpan` / `startToolSpan` / `startToolExecutionSpan`) ② `coreToolScheduler.ts` exec span **abort-as-result snapshot**: tool resolves with normal `ToolResult` after observing `signal.aborted` → snapshot abort flag pass to `endToolExecutionSpan` 让 sub-span 与 cancelled parent tool span 一致 + 加 sanitized `error` reason ③ log/span consistency 4 项 polish；**follow-up to PR#4126 Phase 1，tracked in #4212**——独立 telemetry 工作流，**不在 daemon Wave plan**)
> - ✅ Wave 3 **PR 14b** [PR#4271](https://github.com/QwenLM/qwen-code/pull/4271) MCP guardrail push events + hysteresis — **MERGED 2026-05-18 17:06** (doudouOUC, 9h50m, 33 reviews；最终 **+3329/-266 review 期间 +1505 LOC 涨 +82%**；2 新 typed event：① `mcp_budget_warning` 75%/37.5% hysteresis at manager level (mirrors PR 10 跨抽象层 pattern) ② `mcp_child_refused_batch` coalesced；**PR 14 snapshot 字段保持不变 v1→v2 干净 layer** 验证；新 ACP transport `qwen/notify/session/mcp-budget-event` child→bridge notification；**临门同 PR#4255 widened literal type 问题再来** —— `slow_client_warning` fixture (line 664) widened literal type fail `asKnownDaemonEvent` typecheck，新 PR 14b fixtures (line 776/914) 用 `satisfies` 已修但触老 fixture；wenshao 17:05 APPROVED quote: "state machine + extNotification + bridge republish + SDK predicate/reducer chain holds end-to-end")
> - ✅ Wave 4 **PR 18** [PR#4250](https://github.com/QwenLM/qwen-code/pull/4250) FileSystemService boundary — **MERGED 2026-05-18 05:14** (doudouOUC, **13h55m 创新 daemon 项目最长 lifetime** 破 PR#4247 13h09m；**10 rounds 70 review threads 全 resolved**；最终 +4753/-68（review +1483 LOC）；wenshao R10 quote: "**Ten rounds of review iteration in, all 70 threads resolved, no Critical or Suggestion left dangling**"；review 抓出 13+ Critical + ~50+ Suggestion：`import type` value mismatches 引 ReferenceError CI fail / **TOCTOU symlink substitution** in readText / **writeText 无 TOCTOU 保护** / `edit()` read-modify-write race / **`safeUtf8Truncate` off-by-one** corrupt UTF-8 / OOM gate gap in `readBytes` 不依赖 caller maxBytes / dangling+multi-hop symlink bypass / glob cwd bypass + error classification / `edit()` bypasses encoding/BOM / `hasSuspiciousPathPattern` regex gaps + FP / `enforceReadBytesSize` hard-cap clamp / `stat` ENOENT tolerance / audit message privacy gating；**`assertInodeStableAfterRead` invariant 贯穿 readText/readBytes/edit**——typical chokepoint refactor 范本)
> - ✅ Wave 4 **PR 21** [PR#4255](https://github.com/QwenLM/qwen-code/pull/4255) OAuth device-flow route — **MERGED 2026-05-18 14:05** (doudouOUC, **20h39m daemon 项目史上最长 lifetime** 破 PR#4249 15h20m + **135 reviews 史上最多** + **最终 +6172/-51 daemon 项目史上最大 PR** 破 PR#4249 +5318；review 期间几乎翻倍 +2645/-43；OAuth 2.0 Device Authorization Grant (RFC 8628) brokered through daemon；4 route + 5 typed events + new cap tag `auth_device_flow`；**Runtime locality 严格**：build-time static-source grep 防 11 patterns；**8 设计精彩点**：BrandedSecret 4-way redaction + persist disk-first + poll signal lost-success + transitionTerminal boolean 防 double-fire + broadcastWorkspaceEvent distinct vs PR 16 + dispose wired into shutdown + `oauth_creds.json` 0o600；**临门一脚被 1 TS 类型 inference 卡 12:51 dismissed** (`daemonEvents.test.ts:664` synthetic `slow_client_warning` `type: string` 推导不匹配 `DaemonEvent`) → 13:01 doudouOUC 修 → 13:59 gpt-5.5 /review LGTM → 14:05 MERGED)
> - ✅ Wave 4 **PR 15** [PR#4236](https://github.com/QwenLM/qwen-code/pull/4236) mutation gating helper + `--require-auth` — **MERGED 2026-05-17 12:10** (doudouOUC, 3h06m, 2-3 轮 review 无 Critical 全 nit suggestion；最终 +620/-24；wenshao 真起 `qwen serve` **端到端 verify 4-cell behavior matrix 各 1 次**（review quality 新基线）；`createMutationGate` clean handoff for Wave 4 routes；`CONDITIONAL_SERVE_FEATURES` registry primitive；**解锁 Wave 4 PR 16-21 (6 个 PR)**)
> - ✅ PR 3 follow-up [PR#4225](https://github.com/QwenLM/qwen-code/pull/4225) DaemonSessionClient hardening — **MERGED 2026-05-17 07:05** (chiga0, 3h11m open→merge, +323/-18 测试 141→205 行；多模型 /review pipeline：DeepSeek-v4-pro + claude-opus-4-7 + wenshao 4 轮 review；**chiga0 最终让步把 eager guard 改回 lazy**+ 加 `cursor monotonicity via Math.max`（同 PR#4217 reducer hardening pattern）+ abort-signal propagation + SSE `event.id` validation + test 钉 lazy-guard 防 regression)
> - ⚠️ [PR#4226](https://github.com/QwenLM/qwen-code/pull/4226) typed event schema 竞品 OPEN (doudouOUC, 2026-05-17 03:58, +1398/-18, 测试/生产 1.8x)；与 PR#4217 重叠，开 4 分钟后 PR#4217 即 MERGED；**待 close 或拆 SessionState reducer 部分作 Wave 5 PR 25 提前**
> - ✅ **Bonus** client adapter Stage 0 全 MERGED 2026-05-18：[PR#4203](https://github.com/QwenLM/qwen-code/pull/4203) channel bridge (chiga0, 02:21, +2012) + [PR#4199](https://github.com/QwenLM/qwen-code/pull/4199) IDE connection (chiga0, 02:38, +1676) + [PR#4202](https://github.com/QwenLM/qwen-code/pull/4202) TUI adapter (chiga0, 03:22, +1970)；Wave 5 PR 26 (`flag-gated daemon client adapters`) **提前交付** —— chiga0 按 **3 adapter × 3 stage = 9 PR** 拆分推进
> - ⚠️ **Bonus** Stage 1 wire-up 调整：[PR#4260](https://github.com/QwenLM/qwen-code/pull/4260) TUI harness + [PR#4263](https://github.com/QwenLM/qwen-code/pull/4263) IDE Smoke Test **CLOSED 2026-05-18 06:59**（chiga0 主动；Stage 1 价值被 Stage 2 真接入 production UI 覆盖更彻底，wenshao 抓 command-layer 测试缺口后 chiga0 不再修而是放弃 Stage 1 直接走 Stage 2）；仅 [PR#4261](https://github.com/QwenLM/qwen-code/pull/4261) channel `--daemon-url` 仍 draft OPEN（channel 没 Stage 2 PR）
> - 🔧 **Bonus** Stage 2 experimental flag-gated 2 PR：[PR#4266](https://github.com/QwenLM/qwen-code/pull/4266) `--experimental-daemon-tui` (+664/-16) 真接入 Ink TUI / [PR#4267](https://github.com/QwenLM/qwen-code/pull/4267) `qwen-code.experimentalDaemonIde` config 真注入 webview (+230/-5) —— OPEN draft (chiga0, 2026-05-18 05:06 起；channel stage 2 待开)
> - ✅ **外部** `/demo` debug page [PR#4132](https://github.com/QwenLM/qwen-code/pull/4132) — **MERGED 2026-05-18 08:31** (jifeng, **远古 4 天 24 reviews 终于合**, 最终 +858/-3)；单 HTML browser-based debug UI 是 **Mode B POST+SSE 最薄 client 验证面**；XSS in `logEvent` `tag` 修：识别 `update.sessionUpdate.kind` / `event.type` 从 daemon SSE 上行是 daemon-controlled (而非 user-controlled) → 威胁模型扩到 daemon-emitted poisoned events 后 escape；rebase 过 PR#4247/4250/4251 冲突
> - ✅ **F1** Wave 5 PR 22b/3 + 22b' 合并打包 [PR#4319](https://github.com/QwenLM/qwen-code/pull/4319) `feat(acp-bridge): F1 — acp-bridge package self-sufficiency` — **MERGED 2026-05-19 16:26** to `daemon_mode_b_main` (doudouOUC, ~8h open→merge, +5620/-4710 19 files, 5 rounds review, merge commit `981bc7c7e`；**target `daemon_mode_b_main` 长期 integration 分支**——maintainer 2026-05-19 让 doudouOUC 把剩余 Mode B 工作拆为 F1-F5 feature PRs，**CI ci.yml `pull_request` 不 fire 此 branch**，最终走 `daemon_mode_b_main → main` 周期 merge PR；**4 步 lift**：① `defaultSpawnChannelFactory` + env scrub helpers → `acp-bridge/spawnChannel` (283 LOC) ② `BridgeClient` + `PendingPermission` + `PermissionResolutionRecord` + `MAX_RESOLVED_PERMISSION_RECORDS` → `acp-bridge/bridgeClient` (700 LOC) ③ `createHttpAcpBridge` factory closure + factory-only helpers (`withTimeout` / `canonicalizeExistingAncestor` / `verifyParentWithinWorkspace` / `hasControlCharacter`) + `ChannelInfo` + `SessionEntry` → `acp-bridge/bridge` (3000 LOC) ④ 新 `BridgeFileSystem` 接口 + `BridgeOptions.fileSystem` 字段 + `BridgeClient` constructor 注入 + early-return 委托 in `writeTextFile/readTextFile`（原 22b'，77 LOC + seams）；**净效果 `cli/src/serve/httpAcpBridge.ts` 4682 LOC → 97 LOC** thin re-export shim（channels/base/AcpBridge.ts + VSCode IDE companion 可直接消费 acp-bridge）；**验证 1221/1221 tests** across 35 files (62 acp-bridge + 735 cli serve + 424 sdk-ts) + 11 packages 全 clean；3 deferred 由 PR#4334 全 ship；closes #4329 helper extraction folded `3a6bf3bfd`；deferred #4330 SDK/server timeout coupling declined)
> - ✅ **F2 cleanup PR A** [PR#4411](https://github.com/QwenLM/qwen-code/pull/4411) `perf(core): F2 cleanup PR A — R9/W11/W12/R10 (post-merge follow-ups)` — **MERGED 2026-05-23 to `daemon_mode_b_main`** (doudouOUC, +823/-594 8 files, merge `c6deb58f1`；F2 #4336 post-merge cleanup bucket #4175 item 7；**4 pure-refactor 无行为变化**：① R9 `McpClientManager` constructor 7 位置参 → `(config, toolRegistry, options?: McpClientManagerOptions)` + 顶部 `mkManager(...)` test factory，80 inline construction 变 1-line factory call，test file 净 -104 LOC ② W11 `mcp-transport-pool.ts:acquire()` 抽 `attachPooledSession` + `rollbackReservationOnSpawnFailure` 私有 helper（race-window invariant W10/W77/W90/W111/W125/R24 T17 留 call site 因描述 ordering 不是 helper 本身）③ W12 `session-mcp-view.ts` `applyTools` / `applyPrompts` 预算 filter `Set`s 每 pass 一次（替原每 tool 内 scan includeTools/excludeTools 数组），predicate O(1) per tool；`passesSessionFilter` / `passesSessionPromptFilter` array-based predicate 保 export 不动给 unit test 用 ④ R10/R23 T7 `pid-descendants.ts` 单 `ps -A -o pid=,ppid=` snapshot + 内存树遍历替原 per-pid `pgrep -P` BFS（one fork per node 改 one snapshot）；Windows: 单 `Get-CimInstance Win32_Process | ConvertTo-Csv` snapshot；per-pid 路径保留 fallback for BusyBox `ps` <1.28（无 `-o`）+ distroless container 无 `ps`)
> - ✅ **#4514 capacity backlog T2.9 — prompt absolute deadline + SSE writer idle timeout 双 opt-in flag** [PR#4530](https://github.com/QwenLM/qwen-code/pull/4530) `feat(serve): prompt absolute deadline + SSE writer idle timeout (#4514 T2.9)` — **MERGED 2026-05-26 17:09 to `daemon_mode_b_main`** (doudouOUC, 创建 2026-05-26 02:41 历时 ~14h, 最终 +1458/-151 14 files, target 改至 `daemon_mode_b_main` 不是 main)；**关 [#4514](https://github.com/QwenLM/qwen-code/issues/4514) Tier-2 ⭐⭐ S-sized**——15s heartbeat + AbortSignal local 足够，remote/long-running deployment 需要 explicit app-layer deadline；v0.16-alpha known-limit。两个 opt-in flag **默认 off → single-user loopback 行为 bit-for-bit 不变**：**① `--prompt-deadline-ms <n>`** (env `QWEN_SERVE_PROMPT_DEADLINE_MS`) —— server-side wallclock cap on `POST /session/:id/prompt`；expiry 时 daemon abort AbortController + 返 504 with `errorKind: 'prompt_deadline_exceeded'`；per-prompt body `deadlineMs` 可 SHORTEN below cap 但**不能 EXTEND**（operator 始终是 upper bound）—— IDE 想 30s bail out 服务器 5min cap 可以，反向不行；**关 `httpAcpBridge.ts` 长存的 `FIXME(stage-2)` 注释**——buggy agent 忽略 AbortSignal 握 FIFO 不放问题。**② `--writer-idle-timeout-ms <n>`** (env `QWEN_SERVE_WRITER_IDLE_TIMEOUT_MS`) —— per-SSE-connection idle deadline；`n` ms 内无 write flush 成功（heartbeat 或真事件都算）时，daemon emit terminal `client_evicted` 帧 with `reason: 'writer_idle_timeout'` 并 close；**关 SSE handler 的 "Stage 2 may add" gap**；推荐 ≥30000ms for production（<15000ms 不是 no-op，会 evict 健康连接 —— 第一个 heartbeat refresh `lastWriteAt` 之前 idle timer 已 fire）。**Conditional capability tags** `prompt_absolute_deadline` / `writer_idle_timeout` 仅当对应 flag set 时 advertise，SDK consumer 可 pre-flight 再发 `deadlineMs` body 字段（老 daemon 静默 drop 匹配 v=1 additive rule）。**SERVE_ERROR_KINDS / DAEMON_ERROR_KINDS 两端 mirror +2 个** (`prompt_deadline_exceeded` / `writer_idle_timeout`)；之前 9 / 8 → 现 11 / 10 closed enum 值（关 taxonomy drift insurance test）。**Design rationale**：① `Math.min` cap (operator stays upper bound) ② 直接 `res.write` in idle path 而非走 `writeChain`——chain 可能已 stuck on the very `drain` we're trying to detect，写直接给 terminal frame 一个机会落地 ③ 第二个 timer 与 15s heartbeat 共存：heartbeat = "try to ping every 15s"；idle timeout = "if no flush SUCCEEDED for n ms, force-evict" ④ 默认 off 保 pre-PR contract bit-for-bit。**6 commits 迭代**：① 初版 +13 tests ② **simplify pass** —— `trackWriterIdle` boolean 防 chatty stream 每 write 都 stamp timestamp（数百到数千 writes per session 性能） / 抽 `isPositiveIntegerMs` predicate 让 two new validation block + env parser 一个 source of truth / 抽 `abortableBridgePromptImpl()` helper 消除 3 个 deadline test 的 verbatim copy ③ **Copilot review race-guarantee fix** —— **原 `await bridge.sendPrompt(...)` after `abort.abort()` 不能 guarantee deadline** —— buggy agent 忽略 AbortSignal 时 504 永不 fire 反而 defeats T2.9 整个目的（PR 描述明 claim 关 `httpAcpBridge.ts:2689` FIXME 但实际没关到）；改 `Promise.race(bridge.sendPrompt, deadlinePromise)` —— deadline timer 独立 reject race，504 按时落地 regardless of agent 行为，orphaned bridge promise tail-attached `.catch(() => undefined)` 防 unhandledRejection (买卖几分钟后 buggy agent 终于 settle 时)，`abort.abort` 仍 fire 作 best-effort wind-down 让 cooperative agent free FIFO slot；新 test `still emits 504 when the bridge IGNORES the abort signal (race contract)` 锁 contract；**同时修文档错误**——`<15s` 不是 no-op（4 处修：server.ts 注释 / types.ts JSDoc / serve.ts CLI help / docs/users/qwen-serve.md 表）④ cosmetic 删意外重复 + 软化 `httpAcpBridge.ts:2689` 行号引用为函数名（防 stale） + 改写 stale `FIXME(stage-2)` 注释反映 reality ⑤ **wenshao review fold-in 3 of 6 [Suggestion] 接受 / 3 [推回] 单独 reply thread**：accept `sendDeadlineResponseIfFired` stderr breadcrumb 含 sessionId 让 operator `grep "prompt deadline fired"` triage / accept `parseDeadlineEnv` 不再静默对 whitespace-only env，`Number('')` / `Number(' ')` 走标准 positive-integer reject 匹配 JSDoc "fail loud on typo" 承诺 / accept **`MAX_TIMER_DELAY_MS = 2_147_483_647`** (2^31-1 ms ~24.8 天) 上限校验防 Node `setTimeout` overflow —— >2^31-1 silently compressed to 1ms + TimeoutOverflowWarning，operator 设 30 天 cap `--prompt-deadline-ms 2592000000` 会立即 504 every prompt，本 PR boot 拒 + 清晰错误指向 cap；+25 new tests（6 malformed env / it.each boot validation 0/neg/float/NaN/over-cap / catch-path bare AbortError defense-in-depth / `deadlineMs` body 全 reachable branch / active SSE writes refresh `lastWriteAt` 不触发 idle timer）⑥ **DRY pass** —— `runBudget.ts` 的 `MAX_TIMEOUT_MS` constant export 共享（之前 module-private，避免 two copies 下次 bump drift） + 抽 `emitPromptDeadline504(res, err, sessionId)` 让 deadline-race catch path 与 `sendDeadlineResponseIfFired` defense-in-depth path 共用 wire format + log line + 测试 `afterEach` 集中 scrub 两个新 env var 替代 3 处 per-test `try/finally { delete process.env[...] }`；269/269 tests passing；docs `qwen-serve.md` 加 2 flag + 更新 "phantom SSE connections" known-gap callout；**与 PR#4516 不同 target** —— T2.9 target main 直接进，不走 `daemon_mode_b_main`（这是 #4514 backlog 行 split 心智的延伸：T1.3+T1.4 mutation route 重 plumbing 走 integration branch，T2.9 两 flag 是 server.ts 局部行为 main 即可）
> - ❌ **#4514 capacity backlog T1.3 + T1.4 — author self-close after re-triage** [PR#4516](https://github.com/QwenLM/qwen-code/pull/4516) `feat(serve): POST /session/:id/compress + POST /session/:id/_meta (T1.3 + T1.4 from #4514)` — **CLOSED 2026-05-26 09:25** without merge (doudouOUC, 最终 +2860/-6 22 files 3 commits, target `daemon_mode_b_main`)；doudouOUC self-close 决策原因（quote 2026-05-26 09:25）："Closing this for now after re-triaging #4514. Two parts of this PR are now classified differently: `POST /session/:id/compress` — base behavior 已经可通过 slash-command passthrough 经 `POST /session/:id/prompt` with `/compress` 达到，dedicated route/SSE/SDK helper 是 structured API polish 而非 missing daemon capability；`POST/GET /session/:id/_meta` —— 当前不是 must-have，`POST /session/:id/prompt` 已支持 per-request ACP `_meta` passthrough，session-level KV store 只有具体 channel adapter 真要 daemon 拥有外部 context (chat/thread/sender id) 时才需要；本 PR v1 设计也不把 `_meta` 注入后续 prompt，不完全满足 #3803 原文 'attached to subsequent prompts' 措辞 —— 需更多 product decision。可作 optional convenience/product work 但不应作 Tier-1 必须项停 OPEN；reopen 或拆更小 PR 留给真实下游 client 需要时。" **架构 lesson**: backlog 的 "missing capability" 标记需经 client demand 验证——已有 passthrough surface 算 polish 不是 gap，发现 wire surface gap 不等于要 implement。**关 [#4514](https://github.com/QwenLM/qwen-code/issues/4514) daemon capability backlog Tier-1 唯二两条仍 No PR 的 ⭐⭐⭐⭐ S-sized gap**——按 #4514 的 "if we can only pull 3 things next" 第 1 项推；template 跟 Wave 4 PR 17 (`POST /session/:id/approval-mode`) 同形；bundle 一起因为都是 S 大小 session-mutation 路由共用 mutation-gate plumbing（status / bridgeTypes / bridge / server / capabilities / events / SDK / barrels / tests / docs 一改全改）。**T1.3 `POST /session/:id/compress`** —— 等价 TUI `/compress` 走 HTTP；经新 `qwen/control/session/compress` ACP extMethod 进 agent 的 `GeminiClient.tryCompressChat(force=true)`（server 端总是 `force=true` 匹配 TUI，body 不收 `force` 字段在 v1 schema）；返 `{sessionId, originalTokenCount, newTokenCount, compressionStatus, durationMs}`，`compressionStatus` 是 core 的 `CompressionStatus` enum 字符串名（`'COMPRESSED'` / `'NOOP'` / `'COMPRESSION_FAILED_*'`）；**SSE `session_compacted` 仅在 `compressionStatus !== 'NOOP'` 时 publish** —— NOOP 是 below-threshold history 未动，发会假涨 SDK reducer 的 `sessionCompactedCount`；**双层 concurrency guard**：`CompactionInFlightError` → HTTP 409 `compaction_in_flight`（已有 compress 在 LLM 调用中）/ `PromptInFlightError` → HTTP 409 `prompt_in_flight`（`entry.activePromptOriginatorClientId` 已 set —— 防 daemon 调和 agent `sendMessageStream` 内置 pre-send `tryCompress` race 同一 chat 对象）；non-strict mutation gate（与 `/prompt` parity，与 #4504 `/recap` 模板对齐）；180s timeout (`SESSION_COMPRESS_TIMEOUT_MS`)；**AbortSignal propagation deferred** v1 placeholder signal（operator 等或 `killSession`，cancel surface 自己是单独 design）。**T1.4 `POST/GET /session/:id/_meta`** —— daemon 端 per-session KV 包 for IM / channel adapter（chat_id / sender_id / thread_id），替 wrapper 层 today 的 improvisation；**纯 daemon 端无 ACP roundtrip**，in-memory map on `SessionEntry`，close/kill 时随 `byId.delete(sessionId)` 自动驱逐；**v1 不注入 LLM prompt**（auto-inject deferred until pilot validates prompt format），通过 GET 取 + `GET /session/:id/context` 的 `state.meta` 字段（**capability tag `session_meta` advertised 后 always present 即便 `{}`** —— 避免 old-daemon-vs-empty-bag 歧义）+ 每次写发 `session_meta_changed` SSE event 三个表面暴露；**事件载 FULL new bag 不是 diff** —— 不管 Last-Event-ID gap subscribers 总收敛；validation: key regex `^[a-zA-Z][a-zA-Z0-9_.-]{0,63}$`（400 `invalid_meta_key`）/ reserved `qwen.` 前缀（400 `reserved_meta_key`）/ 总 serialized 8 KB 上限（413 `meta_too_large`）/ `merge:true` 下 `null` value 把 key 设成 null（per JSON 语义，per-key DELETE deferred）；**daemon restart 不持久** v1（load/resume 恢复 session `meta: {}` 起手；随 T2.1 graduate `unstable_session_resume` 再 revisit）。**wire surface added**：+2 cap tag (`session_compress` / `session_meta`) / +1 ACP extMethod (`qwen/control/session/compress`) / +2 SSE event type (`session_compacted` / `session_meta_changed`) / +6 SDK type exports（result × 2 + event data shape × 2 + envelope × 2 全在两个 barrel 重新出 export）/ SDK helpers `DaemonClient.{compressSession,setSessionMeta,getSessionMeta}` + `DaemonSessionClient.{compress,setMeta,getMeta}`；**测试 706 通过**（198 acp-bridge / 270 server / 238 SDK，含 44 new tests：5 T1.3 + 10 T1.4 acp-bridge + 6 T1.3 + 9 T1.4 server + 7 T1.3 + 4 T1.4 DaemonClient + 1 DaemonSessionClient + 5 reducer/narrow + 1 public-surface）；docs: `qwen-serve-protocol.md` 3 new subsections + capability list bump + `state.meta` echo on context section / `qwen-serve.md` 2 new bullets under "What it gives you" + §363 wire-event caveat 更新；**6 parked follow-up**: auto-inject `_meta` 入 prompt context（等 pilot 验证 format）/ `_meta` daemon restart 持久（随 T2.1 graduate）/ per-key `DELETE /_meta/:key`（replace-with-fewer-keys covers v1）/ compress `AbortSignal` propagation / 硬 prompt-side serialization vs in-flight compress（v1 只 fence at compress START）/ `_meta` ETag / optimistic concurrency（last-write-wins v1）
> - ✅ **chiga0 第 4 PR — cross-client real-time sync 5 fixes** [PR#4484](https://github.com/QwenLM/qwen-code/pull/4484) `feat(acp-bridge): cross-client real-time sync completeness (5 fixes)` — **MERGED 2026-05-25 09:28 to `daemon_mode_b_main`**（chiga0, 创建 2026-05-24 16:39 历时 17h, 最终 +835/-105 经 review 期间 expand from initial +195/-18 3 files, target `daemon_mode_b_main`）；离线 cross-client sync audit 发现 8 gap, 本 PR 修 5 个机械性 bridge-layer 的：**① `user_message_chunk` echo on interactive prompt path** —— 之前 cron path (`Session.ts:1402`) + `HistoryReplayer` (`HistoryReplayer.ts:65`) 都发，interactive `Session#executePrompt` 漏 → client B 看不到 client A 发的 prompt 直到 session reload；新 `echoPromptToSessionBus` helper in `bridge.ts` per content block 发一帧，stamped envelope-level `originatorClientId` + `_meta.source: 'bridge-echo'`（SDK 用 `suppressOwnUserEcho: true` 在 originator 端 filter）**② `prompt_cancelled` broadcast in `cancelSession`** —— 之前只 forward ACP cancel notification 给 child + resolve pending permissions，不发 bus event；其他 client 靠"`agent_message_chunk` 没了"的 heuristic 才知道 cancel；emit `prompt_cancelled` envelope 给 peer first-class 事件 **③ `replay_complete` sentinel in `EventBus.subscribe`** —— Last-Event-ID 重连之前只有失败路径 `state_resync_required` 合成终止帧，成功路径无正向信号，client 只能 heuristic 超时 catch-up spinner；emit id-less `replay_complete` 合成帧（空 ring 也发），`data.replayedCount` 是真 force-push 计数（不从 id 算术 derive，state-resync 路径在 ring earliest id 之前留 hole 会 over-count）**④ `originatorClientId` on `session_metadata_updated` envelope** —— `updateSessionMetadata` resolve 了 trusted client id 但只用于 validation，没 stamp 在 broadcast envelope；与兄弟事件 `model_switched` / `approval_mode_changed` 对齐 **⑤ `originatorClientId` on `session_closed` envelope** —— 之前只在 `data.closedBy`，没用 envelope-level `originatorClientId`；加 envelope stamp + 保留 `data.closedBy` for back-compat；**仍 deferred 3 项需 larger design discussion 跟踪 follow-up**：in-session ACP `setModel` bus emit（需要新 ACP `current_model_update` sessionUpdate type 或 side-channel）/ workspace-wide broadcast of non-persisted approval-mode changes / serialize `setSessionApprovalMode` 类似 `entry.modelChangeQueue` 修 concurrent-toggle race / `permission_resolved.originatorClientId` 现是 VOTER's clientId 与 `permission_request` 的 prompt originator 语义不一致；**部署协调注意**：下游 gateway（如 `web-terminal-sandboxs qwen-gateway`）自有 user-message echo workaround 会与 daemon-side echo 产生双重帧，建议先部署 daemon → 再 flip gateway `GATEWAY_ECHO_USER_MESSAGE=false`；`_meta.source: 'bridge-echo'` vs `'gateway-echo'` 让 SDK-side dedup 兜底配置错；291/291 vitest pass + tsc clean
> - ✅ **daemon 团队第 3 作者 ytahdn — 第一个 web client 消费者** [PR#4380](https://github.com/QwenLM/qwen-code/pull/4380) `Feat/daemon react cli` — **MERGED 2026-05-27 01:42 to `daemon_mode_b_main`** (ytahdn, 创建 2026-05-21 06:07 历时 ~6 天)；初版 PR 状态 OPEN CHANGES_REQUESTED 2026-05-21 06:07（ytahdn 是 daemon 团队第 3 作者，继 doudouOUC F-series 与 chiga0 client-facing 双轨之后；+12274/-25 **107 files**, target `daemon_mode_b_main`, 41 commits / 31 reviews）；**新 `packages/web-shell` daemon-backed React web-shell**——接 daemon session / SSE / permission request / slash command completion / model + approval mode 切换 / session resume / memory + MCP + skills + agents view / `/session/:id` URL restore；对齐 CLI/ACP 行为 (`/model --fast` / `/rename --auto` / `/new` / `/reset`)；commit 历史含 `Merge remote-tracking branch 'origin/feat/daemon-ui-core'`（chiga0 SDK daemon UI 分支）+ 多个 `fix(daemon-ui):` 共同 commit —— **这是第一个把 chiga0 SDK 真用起来的 client，验证 #4328/#4353 设计**；**wenshao 2026-05-24 00:32 incremental review**："Re-reviewed 161 files (+24K/-4K) across 22 new commits. 9 parallel review agents + verification + reverse audit. **No new high-confidence Critical findings**" —— 方向健康但旧 thread 未消，CHANGES_REQUESTED 卡在历史 review；**值得注意的风险**：`/session/:id` SPA route 与 daemon API `/session` 同前缀重叠靠 Vite dev proxy 显式 bypass HTML navigation 防 page refresh 错路由到 daemon API endpoint，这种"SPA + REST 同前缀"方案 production deployment 时需要确认 reverse proxy 配置等价（dev 是 Vite，prod 取决于部署）；41 commits / 31 reviews）
> - 🔧 **chiga0 第 3 PR — ACP HTTP transport 接入 ACP 生态** [PR#4472](https://github.com/QwenLM/qwen-code/pull/4472) `feat(daemon): ACP Streamable HTTP transport at /acp [RFD #721]` — **OPEN REVIEW_REQUIRED** 创建 2026-05-24 01:14，最新更新 2026-05-27 07:06（chiga0, **+6098/-316 20 files** 持续 expand，target `daemon_mode_b_main`）；**2026-05-27 进展**：从 +3475 11 files 涨到 +6098 20 files（**review 期间 ×1.75 涨**），仍 OPEN；**R1-R9 9 轮 review iteration** 修 wire-level concurrency 经典坑：R2 reconnect/ownership/leaks / R3 PR bot fold-ins / R4 write-failure ownership+loopback / R5 permission-vote release+disc... / R6 concurrent-prompt abort+zombies / R7 close TOCTOU+pump lifecycle / R9 reconnect prompt survival；**新加 `f101aca1b3` REST parity batch + official extension scheme** —— 跟进 RFD #721 spec 演进，把 daemon REST endpoints (`/session/*` / `/workspace/*`) 同样通过 `/acp` extension namespace 暴露，完成 dual-transport 对偶；最新 `ef59636022` 把 follow-up roadmap 入 docs（acp-bridge 补齐 + `/acp` ...）；**在 `qwen serve` 加 official ACP Streamable HTTP transport ([RFD #721](https://github.com/agentclientprotocol/agent-client-protocol/pull/721)，spec 自身仍是 Draft) 作第二 northbound transport**——挂载 `/acp` 单端点，与现有 REST+SSE API **共存**共享同一 `HttpAcpBridge`+`EventBus`；**架构意义**：之前 daemon "对外讲自创的 REST+SSE 方言；对内（与 `qwen --acp` child）讲真 ACP JSON-RPC over stdio"；这个 PR 让**对外也讲标准 ACP HTTP** —— **Zed / Goose / future ACP-native SDK 可直接驱动 daemon**，不需要懂 qwen-specific REST 方言；3 关键决策：**① dual-transport additive 非替换**——RFD #721 spec 自身仍 Draft 所有 SDK 都没实现 + webui+3 SDK+VSCode companion 依赖 REST surface，env `QWEN_SERVE_ACP_HTTP=0` opt-out（design §6）**② extension namespace `_qwen/...`** 用 ACP spec 保留的 `_` 前缀做 vendor extension：`_qwen/session/set_model` / `_qwen/session/heartbeat`，在 `initialize` 中 advertise（design §5）**③ deferred follow-up**（documented mechanical）：WebSocket upgrade / HTTP/2 multiplexing / 完整 `fs/*`+`terminal/*` forwarding / SSE resumability parity（design §7）；**Wire shape (RFD #721)**：`POST /acp {initialize}` → `200` + capabilities + `Acp-Connection-Id` header / 其他 `POST` → `202`（reply 走 long-lived SSE）/ `GET /acp` (带 conn header) → connection-scoped SSE / (带 conn+session header) → session-scoped SSE 载 `session/update` + agent→client request + response / `DELETE /acp` → `202` teardown；新 `packages/cli/src/serve/acpHttp/` 目录含 `jsonRpc.ts` / `sseStream.ts` / `connectionRegistry.ts` / `dispatch.ts` / `index.ts`；从 `server.ts` 经 `mountAcpHttp(app, bridge, {boundWorkspace})` 挂载；bridges `initialize` / `session/new` / `session/prompt`（streamed update + final result correlation）/ `session/cancel` / `session/load|resume|close` / `session/request_permission`（agent→client→agent round-trip）；15 vitest 全绿（real Express + SSE + fake bridge）+ live daemon smoke 端到端验证（`scripts/acp-http-smoke.mjs`）；**live verification 顺便修了 real bug**：bridge 在 `spawnOrAttach` 时 stamp 自己的 `clientId`（忽略 caller id），所以 per-session call 必须 echo *bridge 的* id（design §10）；**chiga0 第 3 个 PR on daemon_mode_b_main**——继 #4328/#4353 SDK UI 之后开始扩 daemon client-facing surface 第二维：从 web client 渲染层扩到 ACP-native protocol surface）
> - ✅ **chiga0 SDK 侧 shared UI transcript layer** [PR#4328](https://github.com/QwenLM/qwen-code/pull/4328) `feat(daemon): add shared UI transcript layer` — **MERGED 2026-05-22 06:02 to `daemon_mode_b_main`** (chiga0, +6103/-1993 33 files, merge `d0563ecf5`；**chiga0 与 doudouOUC F-series 并行的 SDK 侧 track 起点**——为 web chat / web terminal 加共享 daemon UI 层：typed daemon events → UI events → transcript blocks → framework-free store + 可选 React bindings in `@qwen-code/webui`；动机："web clients should not each reimplement streaming merge, tool preview, permission state, shell output, and reconnect-friendly transcript handling on top of raw daemon SSE frames"；review focus：SDK/webui boundary + browser-safe `@qwen-code/sdk/daemon` export shape + **native local TUI / ACP / channel / IDE defaults 保持不动**——与 maintainer 2026-05-19 web-first 路线对齐；测试覆盖 daemon stream chunk / AskUserQuestion preview / permission request+resolution / store subscription / terminal text sanitization)
> - ✅ **chiga0 SDK 侧 unified completeness follow-up** [PR#4353](https://github.com/QwenLM/qwen-code/pull/4353) `feat(sdk/daemon-ui): unified completeness follow-up to #4328` — **MERGED 2026-05-24 00:51 to `daemon_mode_b_main`**（chiga0, ~4d 17h open→merge, merge `cf5c2453`，**最终 +8531/-110 21 files, 24 commits, 61 reviews（21 CHANGES_REQUESTED + 4 APPROVED + 36 COMMENTED）—— review 强度 daemon 项目第二高，仅次 F2 #4336 的 247**；**#4328 alone 55% → #4353 ~95%**；最初 9 commit PR-A through PR-I 各独立可 review：**PR-A** event coverage 13→28+ types + errorKind + tool provenance **PR-B** serverTimestamps + eventId-based ordering **PR-E** state machine: currentTool / approvalMode / cancellation **PR-C** tool preview taxonomy 4 new kinds + multimodal **PR-D** render contract markdown/HTML/plainText helpers **PR-F** 5 additional preview kinds **PR-G** adapter conformance test framework + 10 fixture corpus **PR-H** WebUI migration **PR-I** developer guide + migration cookbook；review 期间扩展为 **PR-A through PR-K**：PR-K = consume daemon-side subagent nesting context (`2f312a9af3` + self-review hardening `fc35a44694`，原"~150 LOC follow-up"折进本 PR) + **resync-required event handling** (`5e9d9e7d11`，SDK 真正消费 F4 prereq #4360 的 `state_resync_required` 合成终止帧)；**R1-R7 7 轮 review**，收尾阶段从 SDK 消费功能转为 **security + edge-case hardening**：`5c83c904d3` `ensureSafeImageUrl` 限 `data:image/*` only / `599f1acb8d` R4 wenshao+doudouOUC batch / `e394f4935e` R5 **Critical OAuth fragment leak** / `971a69d14c` R6 recovery flow chicken-and-egg + pending / `473614d022` R7 `escapeMarkdownText` 覆盖 `<` + details；最后 6h 静默后 fold R7 → 11 分钟后 MERGED；**剩 §B2 `tool.progress` event (~50 LOC daemon) + §D Multimodal `MessageEmitter.emitUserContent` + `HistoryReplayer` `inlineData`/`fileData` 分支 (~80 LOC Core + ~80 LOC SDK)** 仍为 daemon/Core 跨包 follow-up）
> - ✅ **F1 test split** [PR#4445](https://github.com/QwenLM/qwen-code/pull/4445) `refactor(acp-bridge): F1 test split — lift bridge.test.ts (6861 LOC) to acp-bridge` — **MERGED 2026-05-23 08:46 to `daemon_mode_b_main`** (doudouOUC, ~14h40m, merge `57d04786`, +597/-449 5 files；**F1 第 3 个也是最后一个 deferred follow-up**——`httpAcpBridge.test.ts` 6861 LOC 真正搬到 `acp-bridge/src/bridge.test.ts`；4 个 commit：① pure `git mv` 100% rename detection 保 blame ② extract `acp-bridge/src/internal/testUtils.ts` 抽共享 fixture (`FakeAgent` / `makeChannel` / `makeBridge` / `WS_A` / `WS_B` / `SESS_A`) + 拆 4 daemon-host test 到新 `cli/serve/daemonStatusProvider.test.ts`（唯一真耦合 daemon-host env/preflight cell 的 test，断言 `buildEnvStatusFromProcess` + `canUseRipgrep` + `getGitVersion` + `getNpmVersion` 的真实 cell）③ self-review round 1 加 vitest alias 防读 stale dist + 4 doc/JSDoc polish ④ self-review round 2 修 stale vitest.config.ts comment；**parity preserved**: 181 tests → 177 + 4 = 181；**跨包解析双通道设计**：TypeScript 走 `acp-bridge/package.json` 新 `./internal/testUtils` subpath export（TS `nodenext` 不会从 `exports` reject 后 fall back to tsconfig `paths`，所以 export entry mandatory for type resolution）；vitest 运行时走 `packages/cli/vitest.config.ts` 新 `resolve.alias` 指 `.ts` source —— 编辑 testUtils.ts 不用 rebuild acp-bridge；`internal/` 目录 + `@internal` JSDoc tag = package-private（沿用既有 `internal/stderrLine.ts` 惯例；dist/ 仍 ship 但 alpha-stage 0.0.1 tradeoff，未来可 `stripInternal` 或 `.npmignore` exclude）；2 轮 self-review via `code-reviewer` agent fold 8 findings；291/291 acp-bridge test + 4/4 daemonStatusProvider test + 6742/6767 cli test（16 pre-existing baseline failure 不相关）；`git log --follow` 走回 6861 行 file 的原始 history 验证 blame 保住；**F1 系列正式收尾——3 个 deferred 全 ship 完**)
> - ✅ **F1 follow-up batch** [PR#4334](https://github.com/QwenLM/qwen-code/pull/4334) `feat(serve): F1 follow-up — BridgeFileSystem wiring + #4325 channelInfo fix` — **MERGED 2026-05-20 05:10 to `daemon_mode_b_main`** (doudouOUC, merge `dfa8ca40`, +894/-54 9 files；**3 things batched**：① **BridgeFileSystem wiring** (commit `9560dfc28`) —— ACP `writeTextFile/readTextFile` 现走 PR 18 `WorkspaceFileSystem`（trust gate + symlink resolution + atomic temp+rename + line/limit windowing + unified audit emit）替代 `BridgeClient` inline raw-fs proxy；F1 ship seam 但缺 default wiring，本 PR 让 default `createServeApp` / `runQwenServe` 真注入 adapter；**真关 PR 18 `ws.ts:613` TOCTOU 线** ② **#4325 channelInfo overlap fix** (commit `db8972b81`) —— `closeSession` + `killSession` 用 `channelInfoForEntry(entry)` 替代 module-scoped `channelInfo`，与已有 `setSessionApprovalMode` + `requestSessionStatus` pattern 对齐，消除 channel-overlap windows 的潜在 bookkeeping mismatch；**closes #4325** ③ **Mode preservation + atomic write for ACP writes** (commit `881133407`) —— 解 Copilot review finding：commit 1 前 ACP writes 走 `wfs.writeText` 无 mode 处理，new file 撒 umask-default 0o644 违反 BridgeFileSystem 0o600 契约，existing target mode 不保 (0o600 secret edit downgrade)；fix 引入新 PR 18 primitive `WorkspaceFileSystem.writeTextOverwrite` (~75 LOC 新方法) 通过新 `WriteMode = 'overwrite'` 变体 tolerate ENOENT + skip `expectedHash` CAS gate (不适 ACP hash-less wire)，default 0o600 + mode preservation + atomic temp+rename；contract docstring 重写反映 production posture (write-then-rename atomicity / mode preservation / 0o600 default / **symlink rejection** —— 与 pre-F1 inline-proxy 语义分歧，现与 HTTP `POST /file` from PR 20 一致)；新文件 `bridgeFileSystemAdapter.ts` (110 LOC) + .test.ts (12 tests)；测试 81/81 touched files + 754/754 serve + 62/62 acp-bridge 全绿；**user-visible behavior change for ACP child code**: agent writes through symlinks 现 reject `symlink_escape` (是 security upgrade，pre-F1 inline `BridgeClient.writeTextFile` 用 `realpath`/`readlink` 解 symlink 写 target)；依赖 symlinked dotfile write 的 agent 需要直接寻址 resolved path)
> - ✅ Follow-up [PR#4321](https://github.com/QwenLM/qwen-code/pull/4321) `feat(telemetry): Phase 2 — tool.blocked_on_user + hook spans` — **MERGED 2026-05-21 03:55 to `main`** (doudouOUC, 创建 2026-05-19 09:54 历时 ~42h, 最终 +3287/-99 target `main`；**不在 daemon Wave plan**——Phase 2 of #3731 hierarchical session-tracing plan，built on Phase 1 #4126 + Phase 1.5 #4302；2 新 OTel span：① `qwen-code.tool.blocked_on_user` 追 user think-time（child of tool span，records `decision` / `source` 属性，status UNSET 因 waiting 既非 OK 非 ERROR）② `qwen-code.hook` wraps pre/post-hook fire sites 让 slow hook 与 slow tool 区分；**结构性改 core**: tool span lifecycle 从 `executeSingleToolCall` 移到 `_schedule` validating-loop（覆盖 validating → awaiting_approval → executing 同一 span）2 个新 private Maps on `CoreToolScheduler` 持 span refs by callId；与 claude-code 分歧：claude-code `findLast`-by-type 找 span 并发下错，本 PR 改 span object explicit via `getSpanId(span)`；187/187 tests pass，single-commit revert path 清晰)
> - ✅ **F2** Wave 5 PR 23 shared MCP transport pool [PR#4336](https://github.com/QwenLM/qwen-code/pull/4336) `feat(serve): shared MCP transport pool [F2]` — **MERGED 2026-05-21 15:56 to `daemon_mode_b_main`** (doudouOUC, ~40h open→merge, merge commit `46f8d48f`, 最终 +10308/-147 38 files, **22 commits = 6 atomic feature + 16 review fold-in**, **247 reviews / 30 CHANGES_REQUESTED rounds**；**解 [PR#4306 揭示的 "double MCP discovery in ACP child" 架构发现**] —— `runAcpAgent` bootstrap + per-session `newSessionConfig` 各跑独立 `McpClientManager` 的 2× spawn 浪费，F2 用 **workspace-scoped pool sharing** 修：N session 共享 `(name + fingerprint)` 一个 transport，4 session × `--mcp-client-budget=2` workspace 封顶 2 而非 8；6 feature commit 全落地：① `split McpClient.discover into pure tool/prompt list` ② `McpTransportPool + SessionMcpView`（pool core / fingerprint key / refcount / drain state machine / snapshot replay / generation guard）③ `cross-platform pid sweep`（Linux/macOS `pgrep -P` + Windows PowerShell CIM）④ `wire McpTransportPool into QwenAgent daemon mode`（Config/ToolRegistry/McpClientManager threading + `acpAgent.QwenAgent.mcpPool` ctor + pool lifecycle hook SIGTERM/IDE-close）⑤ `pool-aware status + restart routes`（`GET /workspace/mcp` 加 `entryCount`+`entrySummary` / restart route 加 `?entryIndex=` / **2 新 capability tag `mcp_workspace_pool` + `mcp_pool_restart`**）⑥ `graduate MCP budget guardrails to workspace scope`（新 `WorkspaceMcpBudget` class + `scope:'workspace'` cell + SDK `isWorkspaceScopedBudgetEvent` helper + `broadcastBudgetEvent` fan out to all attached sessions）；**关键不变式**(design doc v2.2)：fingerprint key SHA-256 排除 per-session filters + `canonicalOAuth` 归一 / HTTP/SSE 默认 opt-in（`QWEN_SERVE_MCP_POOL_TRANSPORTS` env 显式开）/ `spawnInFlight` dedupe 5 concurrent → 1 spawn / 30s drain + 5min hard cap 抗 flap（attach 与 `restart()` 时都 cancel，W4/W31 fix）/ `sessionToEntries` reverse index O(refs) / spawn-failure slot release `parseConnectionId` 不用 `startsWith`（W21 false-positive fix）/ snapshot replay on attach 同步防 late attacher 见 empty / generation guard 防 stale `tools/list_changed` 覆盖 post-reconnect snapshot / per-session trust copy `tool.withTrust(this.cfg.trust)` clone / cross-platform pid sweep bounded 2s/256/8 graceful degradation / workspace budget hysteresis 75%/37.5% + refused-batch coalescing per discovery pass；**新工作流 "review at implement"**: 早期 commit 2 写完后 background 跑 `code-reviewer` agent 并行 implement 时 fold bugs；**existing standalone qwen + per-session McpClient path untouched，71/71 mcp-client-manager tests pass unchanged** → 非 daemon 路径零影响)
> - ✅ **F4 prereq** [PR#4360](https://github.com/QwenLM/qwen-code/pull/4360) `feat(serve+sdk): F4 prereq — daemon protocol completion` — **MERGED 2026-05-21 03:11 to `daemon_mode_b_main`** (doudouOUC, +897/-24 11 files, branch off F3 merge tip；2 commits bundled，都是 F4 client-adapter wave 正确渲染前置的协议层 daemon completion：**① #19 stamping** (commit `14637cd79`，chiga0 #4175 comment #19 点的 3 项 daemon-side stamping，SDK 侧 PR#4353 已 forward-compat ready)：`server.ts formatSseFrame()` 在 SSE write boundary stamp `_meta.serverTimestamp`（spread-merge 保留已有 `_meta` key 如 tool_call 的 `toolName`）/ `stream_error` emit stamp `errorKind` via `mapDomainErrorToErrorKind(err)`（unclassified → 字段省略，strictly additive）/ `ToolCallEmitter.ts` 新静态 `resolveToolProvenance(toolName, subagentMeta)` → `{provenance: 'builtin'|'mcp'|'subagent'; serverId?}` 在 emitStart + emitResult + emitError 都 stamp（reconnecting client 可从任何 update event 重新 derive）；**NOT stamped** `session_died` errorKind —— 3 个 emit site 无 classifiable err in scope（`channel_closed` 只带 exitCode/signalCode；`killed`+`daemon_shutdown` 是 user/operator-initiated 非 domain error），threading channel-spawn error 留单独 PR 避免混 protocol stamping 与 lifecycle plumbing **② #15 SSE reducer gap detection** (commit `c1a2f0a78`，关 Ilya0527 #4175 comment #15 raise 的 multi-client state divergence bug)：daemon `eventBus.ts` `subscribe()` replay path 检测 ring eviction（`ring[0]?.id` vs `lastEventId + 1`），`earliest > last + 1` 时 force-push synthetic terminal frame `state_resync_required`（`{reason: 'ring_evicted', lastDeliveredId, earliestAvailableId}`，**无 `id` mirror `client_evicted` synthetic terminal pattern**），replay 在它之后继续；SDK `events.ts` 加 event type + data interface + predicate + widen `DaemonStreamLifecycleEvent` + reducer state fields (`awaitingResync` / `resyncRequiredCount` / `lastResyncRequired?`)，`awaitingResync === true` 时 gate non-terminal events，`RESYNC_PASSTHROUGH_TYPES` set 记录 terminal lifecycle frame 仍 apply（让 resync limbo 期间死的 session 仍 observable）；**consumer recovery contract**：`state.awaitingResync === true` → consumer 调 `loadSession`（out of band）→ `createDaemonSessionViewState({...seedFromLoadSession})` 重建，fresh state 默认 `awaitingResync: false` 隐式清 flag；4 design decisions 都 confirm via AskUserQuestion before coding；全 additive backward-compat（旧 SDK 忽略新字段，旧 daemon 不 emit `state_resync_required` SDK switch fall-through 忽略 unknown type）；tests +27（server +3 / ToolCallEmitter 46 +11 / acp-bridge 108 +5 / sdk 451 +8）；F4 = 原 Wave 5 PR 26 client adapter migration)
> - ✅ **F3** Wave 5 PR 24 PermissionMediator 4 strategies [PR#4335](https://github.com/QwenLM/qwen-code/pull/4335) `feat(acp-bridge): F3 — multi-client permission coordination` — **MERGED 2026-05-20 11:13 to `daemon_mode_b_main`** (doudouOUC, merge `8eeb5100`, +9748/-517 62 files；F3 = 原 Wave 5 PR 24 (`feat(security): client pairing revocation + PermissionMediator`) 拆 2 阶段 第一阶段，**pair-token + revocation API 推迟**为后续 follow-up（consensus 需要的 voter auth）；**4 strategy 完整实现**：① `first-responder` pre-F3 default **wire 字节级保**（snapshot tests 钉） ② `designated` 仅 prompt originator 决，非 originator 403 `permission_forbidden / designated_mismatch`；anonymous prompts（无 X-Qwen-Client-Id）fall back to first-responder ③ `consensus` N-of-M 投票（default `N = floor(M/2)+1`，可通过 `policy.consensusQuorum` 覆盖），first option 到 quorum 赢；中间投票 fan out `permission_partial_vote` SSE 事件 ④ `local-only` 仅 loopback 投票者，remote 403 `permission_forbidden / remote_not_allowed`；**kernel-stamped `req.socket.remoteAddress` 判断，不看 X-Forwarded-For 或任何 header**；**MultiClientPermissionMediator owns ALL pending + resolved state**，bridge 只留 `entry.pendingPermissionIds` 作 fast cap-check index；**PermissionAuditRing** 默认 512 in-memory bounded ring，**NOT routed onto SSE**；2 新 SSE 事件（`permission_partial_vote` consensus 用 / `permission_forbidden` designated/consensus/local-only 用）+ 3 新 typed errors（`PermissionForbiddenError` 403 / `PermissionPolicyNotImplementedError` 501 forward-compat / `CancelSentinelCollisionError` 500）；新 settings `policy.permissionStrategy` + `policy.consensusQuorum`；新 capability `permission_mediation` 总在 `modes: [...]`；SDK `DaemonPermissionPartialVoteEvent` / `DaemonPermissionForbiddenEvent` types + reducer `permissionVoteProgress` / `forbiddenVotes` 扩展；**5 hardness invariants**：N1 `mediator.request()` 同步注册 pending in Promise executor 防 forgetSession race / N2 `resolveEntry` cleanup hardened 即使 emit/audit throw Promise 也 settle / **N3 deliberately preserved 旧 `permission_resolved.originatorClientId === voter.clientId` 不一致**保 wire 字节 compat 用 regression test 钉 / O5 voter cancel `__cancelled__` sentinel collision against agent labels 在 request issue time throw / O8 first-responder bit-for-bit；Tests: 35 mediator unit + 10 audit ring + 55 SDK reducer + 3 bridge integration + 旧 httpAcpBridge.test.ts snapshot suite 全绿；**migration 默认行为不变**——缺省 keep first-responder，operator 写 4 string 之一 to settings.json opt in，daemon restart 生效；5 deferred follow-up：audit query route / pair-token+revocation API / live-reload / decision persistence / Hook layer)
> - ✅ Follow-up [PR#4367](https://github.com/QwenLM/qwen-code/pull/4367) `feat(telemetry): support custom resource attributes and add metric cardinality controls` — **MERGED 2026-05-21 05:54 to `main`** (doudouOUC, 创建 2026-05-20 19:02 历时 ~11h, 最终 +1897/-60 13 files, target `main`；**不在 daemon Wave plan**——closes #4365，#3731 P3 line（resource attribute policy + cardinality controls）；2 个耦合 OTel 能力：**① custom resource attributes** —— `OTEL_RESOURCE_ATTRIBUTES` / `OTEL_SERVICE_NAME` 标准 env var 现生效（之前 `autoDetectResources: false` 静默丢弃两个标准 env var）+ 新 `telemetry.resourceAttributes` setting，operator 可给每个 span/log/metric 打 `team`/`env`/`cost_center` tag **② metric cardinality controls** —— `session.id` 从 OTel `Resource` 移走（之前 auto-attach 到每个 metric data point → Prometheus/ARMS 上 unbounded time-series fan-out），gated 在新 opt-in `telemetry.metrics.includeSessionId` toggle 后；span + log 仍带 `session.id` 做 trace/log correlation；reserved key（`service.version` 防 spoof telemetry version / `session.id` runtime-injected Resource 级放置会绕过 cardinality toggle）从 env + settings 两 source strip + `diag.warn`；**⚠️ breaking change**：metrics 默认不再带 `session.id`，恢复旧行为需 `QWEN_TELEMETRY_METRICS_INCLUDE_SESSION_ID=true` 或 settings —— 与 #4333 同类 release-note 候选；130 new+updated unit tests + 1193 telemetry/config tests pass；design doc `docs/design/telemetry-resource-attributes-design.md` 含 layering / precedence chain / reserved-key policy / 与 Claude Code 实现对比)
> - 🔧 Follow-up [PR#4333](https://github.com/QwenLM/qwen-code/pull/4333) `feat(core): atomic write rollout for credentials, memory, config, JSONL` — **OPEN REVIEW_REQUIRED** 2026-05-19 16:16 (doudouOUC, +643/-145 31 files, target `main`；**不在 daemon Wave plan**——**#4095 Phase 2** atomic write rollout (Phase 1 = #4096)，closes **#3681** JSONL session writer durability；10 commits（6 tier migration + 1 cosmetic + 3 Codex-review-round 真 bug 修）；**重度改 core 全路径** OAuth 凭证 / memory state / config / JSONL session writer / logger / LSP 全用 `atomicWriteFile` / `atomicWriteFileSync` 替换 bare `fs.writeFile/appendFile`；**5 个 release-note 候选行为变化**：① `qwenOAuth2.ts:982` cacheQwenCredentials `0o644` → `0o600` forced ② OAuth token storage 多处 `forceMode: true` heal 历史 `0o644` → `0o600` ③ `jsonl-utils.writeLine/Sync` 加 `flush: true` **+几 ms latency per assistant turn / tool call**（消除 kill -9 后 `}{` glue）④ `sharedTokenManager.saveCredentialsToFile` 去 5s `withTimeout` 修一个 silent token-overwrite race（NFS hang 现阻塞 refresh 而非 rollback to stale token）⑤ `NativeLspService.applyTextEdits` 显式 `W_OK` check + 只 ENOENT 当 new-file 防 atomic rename 绕 file-level write perms；**3 Codex review round 抓真 bug**: `forceMode` without `mode` 静默 downgrade perms / `logger.test.ts` `vi.resetAllMocks()` blank shim 测试 by coincidence 过 / `NativeLspService` swallow read errors / chmod 0444 仍可被 atomic rename 替换 / `withTimeout` 包 atomic write 触 rename race；**验证强度**: 630 tests + 17-test integration script + 真 tmux `npm run dev` kill -9 e2e (JSONL 10/10 clean + `/resume` 成功)；`httpAcpBridge.ts:1274` daemon 端 hand-rolled atomic write deliberately NOT in scope，留 follow-up 折叠到 shared helper；**对发版风险评估的补充**：jsonl flush:true 是唯一全模式 user-perceptible 性能变化，方向均为 crash-atomic hardening + 修历史 perm leak)
> - ✅ Wave 5 **PR 22b/2 design slice** [PR#4304](https://github.com/QwenLM/qwen-code/pull/4304) `BridgeOptions` lift + `DaemonStatusProvider` seam — **MERGED 2026-05-19 01:27** (doudouOUC, **1h57m** open→merge, merge commit `68e3ec988a`，+852/-371 11 files；wenshao 1 轮 CHANGES_REQUESTED → 4 轮 inline 修 → qwen-latest /review LGTM → MERGED；**3-stage 改 4-stage**：原 PR 22b/2 implementation lift 拆为 22b/2 design slice (冻 contract) + 22b/3 mechanical bulk lift (~3000 LOC, 1-2h IDE-driven `git mv`)；6 design decision baked in JSDoc：positional 参 / 全 envelope 返 / 无 abort/timeout / 单接口含 2 方法 / 可选 + idle fallback / acpChannelLive 由 bridge 传参；2 文件移：`BridgeOptions` interface (~150 LOC) → `acp-bridge/src/bridgeOptions.ts` + `buildDaemonPreflightCells` + `safeCheck` (~210 LOC byte-identical) → `cli/src/serve/daemonStatusProvider.ts`；新 factory `createDaemonStatusProvider()`；**Mode A in-process consumer 可省 `statusProvider?` 不崩** —— 对齐 PR 12/13 "idle status is queryable"；701/701 vitest pass；**Wave 5 PR 22 现 3/4 MERGED，仅剩 22b/3 mechanical bulk lift**)
> - 🔧 Follow-up [PR#4305](https://github.com/QwenLM/qwen-code/pull/4305) #4291 post-merge fix（7 threads）— **OPEN + CHANGES_REQUESTED** 2026-05-18 23:50 (doudouOUC, +454/-116 5 files；qwen-latest review on **MERGED #4291** 后 7 项 hardening：① late-poll observer 解构原始字段不再持 entry/BrandedSecret/cancelController（**memory + secret retention**）② 抽 `callerIsDeviceFlowInitiator(view, callerClientId)` 共享 helper 3 处 inline copy 换掉（**DRY+回归预防**）③ timer fire `DeviceFlowPollTimeoutError` 单实例复用 abort.reason+reject（polish）④ `err.name` 走 `sanitizeForStderr`（**log injection 姊妹堵**与 `oauthError`）⑤ timeout 路径 audit hint 用 `result.hint` 与 SSE 一致 ⑥ inline `QWEN_SERVE_DEBUG` → 现有 `isServeDebugMode()` helper + `?? ''` 死代码删（DRY+dead code）⑦ late-rejection observer `name + length` 模式对称 provider catch 不再 slice 原 message（**secret leak**）；新测试 hard-negative-assert secret 不入 stderr + hostile `Error.name` sanitization；OAuth 史诗续：**+8032 LOC line if PR 21+#4291+#4305 全合**)
> - ✅ Follow-up [PR#4306](https://github.com/QwenLM/qwen-code/pull/4306) unbreak E2E after #4271（capabilities + clientCount）— **MERGED 2026-05-19 01:16** (doudouOUC, **41m 极短** — qwen-latest /review 一把 APPROVED merge commit `7daf616e8b`；+48/-40 2 文件 integration test only；**2 regressions** from PR#4271 MCP guardrail events 落地后 main E2E 每跑必挂：① capabilities envelope test 漏 `mcp_guardrail_events` tag —— **第 9th drift** 同 #4268/#4284 class；integration test hand-maintained list 是 unit baseline + production registry 之外**第三处 hand-maintained 数据源** → "三套" 实际是 4 套 ② `clientCount === pgrep_observed` 不再成立——ACP child 跑 **2 个 `Config` 对象**（`runAcpAgent` bootstrap + per-session `newSessionConfig` 各一）每个独立 `McpClientManager`；fix 把 `MCP_SERVERS_CONFIGURED * 2` 编码为 literal tripwire；**架构发现**：**double MCP discovery in ACP child** 真浪费 spawn cost —— 值得 #4175 follow-up gate bootstrap MCP behind daemon-mode flag 或 refactor `newSessionConfig` 共享 bootstrap `McpClientManager`)

详 [§06 Roadmap & Ecosystem](./06-roadmap.md)。

---

## 六、阅读指引

| 角色 | 推荐路径 |
|---|---|
| **想知道这是什么** | §01（本章）|
| **想知道何时 ship 什么** | §06 Roadmap |
| **想知道为什么这么设计** | §02 Design Decisions |
| **想集成 client** | §03 HTTP API → §04 Client Experience |
| **关心安全** | §05 Security & Permission |
| **商业平台集成方** | §06 §四 External Reference Architecture |

---

下一篇：[02 — Design Decisions →](./02-architectural-decisions.md)
