# 三方 Code Agent 周报 · 2026-W22（05-24 ~ 05-31）

> 对比 **Qwen Code / OpenCode / Codex** 最近一周**非 daemon** 进展。Qwen Code 的 daemon 系列（F-series / ACP HTTP / web-shell / MCP bridge 等）已在 [daemon 设计文档](../qwen-code-daemon-design/README.md) 单独跟踪，本周报不重复。
>
> 数据来源：GitHub merged PR / commit（QwenLM/qwen-code main 分支、sst/opencode dev 分支、openai/codex），统计窗口 2026-05-24 00:00 ~ 2026-05-31 23:59 UTC。

## 0. 一句话概览

| 项目 | 本周主线 | 体量 | 信号 |
|---|---|---|---|
| **Qwen Code** | **两次发版 v0.16.2 + v0.17.0**；compaction 架构二次重构（BREAKING）；computer-use 零配置接入 | ~40 非 daemon PR | 核心能力快速迭代 + 连发版本 |
| **OpenCode** | **acp-next 新实现集中攻坚并 promote**；Desktop v2 打磨；location-scoped config | ~40 实质 commit（+ 大量 stats 网站 / generate 噪声）| ACP 重写 + 桌面端 + 公开 leaderboard 运营 |
| **Codex** | **193 PR 超高速**；Guardian 行动审批守门深耕（LLM 逐动作判风险）；goal/thread-idle 生命周期；Python SDK beta 独立发布 | **193 merged PR** | OpenAI 级研发密度，平台化 + SDK 化 |

**速度差异**：Codex 一周 193 个 merged PR（OpenAI 内部团队节奏，单 PR 粒度极细），Qwen Code ~40，OpenCode ~40 实质提交（但混大量 stats 网站运营 commit）。三者绝对体量差约一个量级。

---

## 1. Qwen Code（非 daemon）

### 🔥 重大变化

| PR | 主题 | 说明 |
|---|---|---|
| [#4599](https://github.com/QwenLM/qwen-code/pull/4599) | **compaction 二次重构（BREAKING）** | `replace tail-preservation compaction with summary + restoration attachments`（+3034/-1315）—— 紧接 #4345 三档 ladder 后再换 compaction 策略：从"保留尾部"改为"摘要 + 恢复附件" |
| [#4345](https://github.com/QwenLM/qwen-code/pull/4345) | **三档梯度 auto-compaction（BREAKING）** | warn / auto / hard ladder + hard-tier API 拒绝前 force rescue；`chatCompression.contextPercentageThreshold` 设置移除（详 [context-compression deep-dive §六](../context-compression-deep-dive.md)）|
| [#4590](https://github.com/QwenLM/qwen-code/pull/4590) | **computer-use 零配置内建** | `zero-config built-in via open-computer-use MCP`（+4869）—— 通过 MCP 把 computer-use 做成开箱即用，对标 Claude Computer Use |
| [#4381](https://github.com/QwenLM/qwen-code/pull/4381) | **worktree Phase D** | 启动 `--worktree` flag + symlinkDirectories + PR refs（+3719）|
| [#4379](https://github.com/QwenLM/qwen-code/pull/4379) | **Feishu (Lark) channel adapter** | 第 4 个 IM channel（DingTalk / WeChat / Telegram 之外）（+3961）|

### 行为变化 / 安全

- [#4547](https://github.com/QwenLM/qwen-code/pull/4547) 默认开 auto-dream / auto-skill + `/memory` toggle（**默认值翻转**）
- [#4386](https://github.com/QwenLM/qwen-code/pull/4386) command substitution 从 hard-deny 改 `ask`（#4093）
- [#4502](https://github.com/QwenLM/qwen-code/pull/4502) headless / 非交互 runaway-protection guardrails
- [#4376](https://github.com/QwenLM/qwen-code/pull/4376) AUTO classifier block 时 emit PermissionDenied hooks
- [#4371](https://github.com/QwenLM/qwen-code/pull/4371) strip 额外危险解释器规则；[#4324](https://github.com/QwenLM/qwen-code/pull/4324) 限制 background agent 并发

### 工程 / 体验

- [#4477](https://github.com/QwenLM/qwen-code/pull/4477) dense inline panel + LiveAgentPanel 键盘导航（详 [subagent-display deep-dive §六.14](../subagent-display-deep-dive.md)）
- [#4544](https://github.com/QwenLM/qwen-code/pull/4544) 拖放/粘贴多文件路径自动 `@`；[#4487](https://github.com/QwenLM/qwen-code/pull/4487) require whitespace before `@`
- [#4565](https://github.com/QwenLM/qwen-code/pull/4565) skill-based RT optimization telemetry 基础（+2404）
- [#4394](https://github.com/QwenLM/qwen-code/pull/4394) `.qwen/QWEN.local.md` 作 project-local context；[#3826](https://github.com/QwenLM/qwen-code/pull/3826) track model-sent slash command history

### 发版

- **v0.16.2**（#4571, 05-29）+ **v0.17.0**（#4626, 05-29）—— 一天连发两个 minor

---

## 2. OpenCode

> `dev` 分支 squash-merge；本周提交里**约一半是 stats 公开 leaderboard 网站 + `chore: generate` 自动产物**，过滤后实质工程提交 ~40。

### 🔥 主线：acp-next 新实现集中攻坚

一周内密集推进 ACP 第二代实现：

```
#29320 acp-next session lifecycle
  → #29327 event routing
  → #29333 stream tool updates
  → #29664 implement prompt
  → #29656 permission events
  → #29709 first-session startup
  → #29713 warm switches
  → #29719 smoke parity gaps
  → #29929 promote next implementation  ← 晋升为默认
```

这是 OpenCode 本周最聚焦的一条线——**把 ACP client 支持整个重写并切换**。对比：Qwen Code 的 ACP 是 daemon 第二 transport（chiga0 #4472，daemon 侧），OpenCode 是 client 侧 ACP 实现换代。

### Desktop v2 + 多窗口

- #28788 refine desktop v2 home and session controls
- #29526 toggling tabs layout / #29523 tab close button
- #29285 server SDK + sync state 全局化；#29328 V2 titlebar 删 session 同步缓存

### 核心架构

- #29625 location-scoped config loading + location layer 隔离
- #23068 session metadata support
- default agent plugin / v2 default agents
- #29068 移动 database schema ownership（refactor）
- effect CLI scaffold（迁 Effect 框架信号）

### Provider / 稳定性

- 多 provider **Opus 4.7+ adaptive reasoning** 支持（sap-ai-core / vertex / gateway）
- #29837 retry stalled SSE streams / #29852 serialize mcp auth mutations
- #29861 session switcher plugin / #29612 workspace management dialog / #29493 plugin dispose hook

---

## 3. Codex

> **193 merged PR**——OpenAI 内部高密度节奏，单 PR 粒度极细（常见 +0/-X 拆分式重构）。主题归类：

### 🔥 Guardian 行动审批守门（jif-oai 主力，本周最大投入）

**先澄清：Guardian 不是「PR 代码评审」，而是 Codex 的「行动审批守门员」**——agent 每次准备跑命令 / 写文件 / 删除 / 联网之前，先把这个动作交给一个独立的 Guardian LLM 评一遍：这事**有多危险**（low/medium/high/critical）+ **用户到底授权了没**（high/medium/low/unknown），再决定放行 / 追问 / 拒绝。源码是 `codex-rs/core/src/guardian/` 一整个独立模块（专属 prompt + policy 模板）。

可以理解成：**比"规则匹配式权限弹窗"更聪明的一层——它会先用只读工具取证再判**（比如删文件前先 `ls` 看目标是不是空的，联网前先看 git remote 判断仓库是不是公开的），还专门防 prompt injection（把工具输出当"不可信证据"，不被里面"求放行"的话术带跑）。

本周做的是给这个系统**省钱 + 装仪表盘**：

- **省钱（prompt cache 复用）**（#24803/#24891/#24893/#24894/#24895）—— 之前 fork 出来的 review session 各用各的 cache key，本该共享缓存的却没共享，白烧 token；改成稳定 key 后命中率上去了
- **装仪表盘（metrics）**（#24897）—— 加 OpenTelemetry 指标（review 次数 / 延迟 / token 消耗 / 最终是放行还是拒绝），方便灰度时盯着 Guardian 的行为按模型、动作类型、失败原因分类对比
- **修隔离**（#24714）—— auto-review 线程不该继承父会话的 notify hook，清掉防泄漏

### goal / thread-idle 生命周期

```
#24744 thread idle lifecycle hook
  → #24928 wire task completion into thread-idle
  → #24915 thread start contributor facts
  → #24916 turn error lifecycle contributor
  → #24925 gate goal tools by thread eligibility
  → #24918 internal model context fragments for goal steering
  → #25095 goal usage limits from turn errors
```

对比 Qwen `/goal`（详 [goal-command deep-dive](../goal-command-deep-dive.md)）：Codex 把 goal 与 thread 生命周期深度绑定。

### Python SDK beta 独立发布

- #24828 independent beta release → rename `AppServerConfig` → `CodexConfig`（#24800）+ Codex names across surface（#24804）+ 文档/包元数据（#24836）—— **SDK 产品化**

### 扩展 / 插件平台化

- #24723 feature-gated standalone **image generation extension**（+1056）→ #24972 route through native image pipeline
- #24700 ui visibility meta for tools / #24813 TurnItemEmitter to tool calls
- #24977 runtime extra skill roots API / #24996 filter plugin install suggestions by installed apps / #24707 runtime enablement for remote plugins

### multi-agent v2 + 平台

- #24903 reap stale multi-agent slots / #25267 rename assignment tool
- #24698 expose MCP server info in status / #24964 wait for MCP readiness
- #24831 Windows sandbox provisioning setup command（+469）
- #25027 `/archive` slash command + #25021 thread archive CLI（+697）

### 清理

- debug-client 整套移除（#25063 ~ #25070，7 连删 PR，纯删除 ~1500 LOC）

---

## 4. 横向对比

| 维度 | Qwen Code | OpenCode | Codex |
|---|---|---|---|
| **本周焦点** | compaction 重构 + computer-use + 连发版本 | acp-next 重写并切换 + Desktop v2 | Guardian 行动审批守门 + goal lifecycle + SDK beta |
| **ACP 动向** | daemon 第二 transport（#4472，daemon 侧）| **client ACP 实现换代并 promote** | 无明显 ACP 动作 |
| **多模态** | computer-use 零配置（open-computer-use MCP）| — | image generation extension |
| **权限/安全审批** | permission flow + PermissionMediator（规则匹配 + 多 client 投票）| — | **Guardian = LLM 逐动作判风险的审批守门，会先取证再放行** |
| **PR/代码评审** | `/review` 9-agent fan-out（#4477 展示层）| — | （Guardian 不属于此类）|
| **SDK** | canUseTool timeout（#4491）/ CLI chunks（#4541）| server SDK 全局化 | **Python SDK 独立 beta 发布** |
| **发版节奏** | v0.16.2 + v0.17.0（一天双发）| v1.15.13 rolling | 持续 rolling（193 PR）|
| **运营信号** | Feishu channel（中国生态）| 公开 stats leaderboard 网站大力运营 | platform / extension 化 |

## 5. 值得 Qwen Code 关注的点

1. **OpenCode 的 acp-next 换代**：OpenCode 把整个 ACP client 实现重写并 promote，与 Qwen daemon 的 ACP HTTP transport（#4472）形成 ACP 生态两端呼应——**ACP 正成为多家共识**，Qwen 押注 dual transport 方向得到旁证。
2. **Codex 的 Guardian 行动审批守门**：这其实对标的是 **Qwen 的 permission flow / PermissionMediator，不是 `/review`**。差别在于——Qwen 现在是"规则匹配 + 多 client 投票"决定要不要批一个动作；Guardian 是让一个 LLM **逐动作判风险**，还会先用只读工具取证（删文件前先看目标是不是空的）再决定放行/追问/拒绝，并显式防 prompt injection。如果 Qwen 想让权限审批从"硬规则"升级到"会思考、会取证"，Guardian 的思路值得参考；它本周还顺带做了 prompt cache 复用（省 token）+ OpenTelemetry 指标（可观测），这两点对任何 LLM 子系统都通用。
3. **Codex Python SDK 独立 beta**：Qwen 的 SDK 是 TypeScript 单一栈，Codex 已铺 Python SDK 产品线——多语言 SDK 是平台化必经路。
4. **三家都在收敛 compaction / context 策略**：Qwen 一周内两次重构（#4345 ladder → #4599 summary+restoration），说明 context 管理仍是核心未定型区域。

---

> **免责声明**：本周报基于公开 GitHub 数据按 commit message / PR title 归类，未逐 PR 阅读源码；体量数字（+X/-Y）为 GitHub 报告值。OpenCode 提交含大量自动生成产物与 stats 网站运营，"实质 ~40" 为人工过滤估算。Codex 单 PR 粒度极细，193 这一数字含大量拆分式小 PR，不宜与 Qwen/OpenCode 的 PR 数直接等量对比。
